---
title: Status map of all 8 angles (+ MCSP)
category: synthesis
tags: [status-map, mcsp-def, meta-complexity-thesis, mcsp-deconditioning, barrier-evasion-overstated, mcsp-easy-naturalproof, locality-barrier, goldilocks-structure, route-a-route-b, hirahara-ilango-2025, chen-2018-maxip, majmaj-capp-route, majmaj-symmetry-deflated, chen-ren-2020, hmpt-1993, mining-program, tell-2018-quantified, dilv-2024, chen-williams-2019, maxip-shave-obstruction, route-a-compression-crux, route-a-variant-gap, route-a-dual-locality, route-a-owf-escape, choprs-2020, ilango-2020, s1-derandomization, s1-hos18-precedent, s1-chebyshev-moce-oracle, s1-meta-vs-lowdegree, s1-necessary-insufficient, s1a-chebyshev-phantom, s1a-lupanov-only-rng, s1a-antiprox-obstruction, s2-irritable-witness-obstruction, witness-needs-explicit-lb, mining-nw-seam, mining-worst-vs-average, css-2018, kane-williams-2016, mining-gap-two-dimensional, chen-tal-wang-2025, amano-2020, non-amplification-also-class-drifts, stv-2001, crty-2023, goldreich-rothblum-2019, s3-locality-escapes-witness-barrier-does-not, locality-vs-witness-orthogonal, hos-2018, avg-worst-2x2-cell, threshold-aggregation-obstruction, cllo-2021, kumar-2023, five-cycle-synthesis, ac0-lb-existence-passed, ac0-natural-proofs-wall, ppoly-recognizable, ac0-tight-window, sipser-rst-2015, parameterization-correction, second-loop-cycle-1, recognizable-stronger-than-natural, williams-constructive-theorem, largeness-vs-constructivity, triple-conjunction-wall, williams-algorithmic, second-loop-cycle-2, tension-not-definitional, constructivity-forcing-nexp-specific, easy-witness-mechanism, cwy-2023-blackbox, dhayal-impagliazzo-2020-unique, recognizer-needs-mcsp, wall-loosened-not-fallen, second-loop-cycle-3, boundary-case-resolved, single-lb-implies-class-lb, williams-iff-vicinity, targetability-circularity, wall-pinned-to-targetability, second-loop-cycle-4, second-loop-synthesis, wall-pinned-not-moved, q-targetability-resolved, williams-iff-non-targetable, vicinity-does-not-retighten, wall-stays-loosened, retargeting-circular-confirmed, third-loop-cycle-1, gate-1-automatic, two-gates-overcounted, lupanov-plausibly-nonnatural, lupanov-largeness-violated, naturalization-open-question, wall-collapses-to-derandomization, gate-1-residual-is-naturalization, third-loop-cycle-2, ilango-recognizable-modification-loses-hardness, recognizable-variations-one-sided, tight-window-upper-bound-linchpin, b-reduces-to-a, b-existence-yes-b-usability-likely-no, wall-collapses-to-a, third-loop-cycle-3, lifting-needs-expensive-and-small-gap, expensive-tensions-small-gap, a-is-intrinsic-not-lupanov-artifact, a-is-a-balanced-point, alternative-function-no-bypass, third-loop-cycle-4, two-dim-to-one-dim-wall, balanced-point-non-compositional, decouple-expense-from-gap, tension-is-lupanov-observed-not-proven-inherent, gate-1-soft-three-directions, mining-face-forward-pointer, third-loop-synthesis, third-loop-cycle-5, goldreich-wigderson-dichotomy, tc0-extractor-enables-error-reduction, tell-quantified-derand-is-a-real-lever, wire-frontier-quantitative-gap, black-box-natural-proof-barrier, sharp-threshold-at-phi, mining-face-structurally-different, two-faces-two-np-variants, fan-li-yang-2022, chen-jin-williams-2020, fourth-loop-cycle-1, williams-method-is-non-black-box, n2-wall-and-bbnp-are-two-axes, two-barriers-two-axes-mining-face, williams-sym-plus-coupling, capp-route-is-black-box, chen-tal-wang-inhabits-target-quadrant, target-quadrant-not-at-mining-regime, chen-tal-wang-2026, fourth-loop-cycle-2, williams-lb-inherently-worst-case, avg-case-needs-separate-amplification, capp-implies-strong-avg-case, two-senses-of-non-black-box, williams-lb-is-black-box-constructive, cycle-12-walk-back, within-quadrant-not-barrier-free, fly-largeness-residual-open, fourth-loop-cycle-3, fly-needs-no-largeness, fly-soundness-on-random, nonlargeness-not-an-fly-escape, rr-and-fly-partition-by-largeness, cwy-bridge-two-faces, mining-face-naturalproofs-blocked, fourth-loop-cycle-4, two-routes-converge, nexp-lb-route-naturalproofs-closed, np-skin-two-variants-two-axes, s1a-is-the-live-thread, mining-redirects-to-s1a, fourth-loop-synthesis, fourth-loop-cycle-5, derandomization-equiv-needs-largeness, nonlarge-derandomization-equiv-exists, a-is-apepp-style-construction, korten-apepp-hard-truth-table, n2-wall-lupanov-tension-parallel, ilango-2025-derandomization-link, fifth-loop-survey, fifth-loop-cycle-1, fifth-loop-cycle-2, a-plausibly-in-apepp-not-complete, unique-equiv-is-class-lb-only, two-axis-robustness-face-asymmetric, apepp-mcsp-connection, fifth-loop-cycle-3, apepp-mcsp-np-one-triangle, locality-barrier-is-the-bridge, a-is-two-balanced-points, apepp-derand-needs-lb, fifth-loop-cycle-4, constructible-implies-recognizable, derandomization-destroys-nonrecognizability, recognizer-strength-no-escape, noncompositionality-mechanistic-asymmetric, fifth-loop-cycle-5, fifth-loop-synthesis, no-missed-connection-moved-wall, wall-sharpest-mechanistic-form, a-remains-open-construction, sixth-loop-cycle-1, fly-constructivity-is-corp-bptime, barrier-ceases-above-conp, recognizer-strength-escape-is-open, cycle19-no-escape-was-overextension, fpnp-escape-not-breakthrough, wall-purely-structural-for-fpnp, meta-obstruction-fp-specific, two-faces-constructivity-axis, fpnp-converges-to-williams-template, sixth-loop-cycle-2, s1a-class-is-dnf-easy-sat, williams-template-convergence-asymmetric, structural-obstruction-is-full-balanced-point, cycle21-elpnp-lb-under-specified, measure-hypothesis-implies-pnp, rsc-martingale-naturalproof-equivalence, measure-pivot-rediscovers-natural-proofs, positive-measure-means-np-pseudorandom, measure-relativization-barrier, dimension-weaker-than-measure, disjoint-pairs-characterization, point-to-set-principle, sixth-loop-cycle-3, disjoint-pairs-stronger-not-easier, disjoint-pairs-relativize, disjoint-pairs-re-collapse-to-inseparable-pair, disjoint-pairs-inherit-rsc, disjoint-pairs-bridge-to-proof-complexity, proof-complexity-face-barriered, logic-for-p-implies-pneqnp, descriptive-escapes-relativization, cpt-separator-not-pneqnp, descriptive-bridges-to-proof-complexity, descriptive-re-collapse-weak-model-lb, logic-for-p-harder-not-easier, sixth-loop-cycle-5, seventh-loop-cycle-1, gct-escapes-three-barriers, gct-flip-is-construction-lock, mulmuley-complexity-barrier-equals-witness-barrier, barriers-and-construction-are-complementary, flip-construction-objects-are-nphard, vnp-separation-not-pneqnp, gct-flip-needs-deep-open-math, seventh-loop-cycle-2, capp-template-productive-for-easy-sat, cycle22-sat-vs-capp-conflation, clw-lb-non-explicit-refuter, clw-lb-subexp-not-tight-window, construction-lock-localized-two-corners, cwy-blackbox-unavoidable-from-capp-angle, ip-gadget-lifting-proof-complexity-2025, seventh-loop-cycle-3, depth-axis-barrier-profiled-not-untouched, krw-may-evade-natural-proofs, depth-transfer-gap-p-vs-nc1, direct-sum-is-depth-face-of-noncompositionality, alman-liang-2025-rigidity-stuck-barrier, chen-hu-ren-2026-algebrization-via-communication]
status: reference
last_touched: 2026-08-23
---

# Status Map — 8 Angles + MCSP

| # | Angle | Status | Where it dies |
|---|---|---|---|
| 1 | Barriers / unified 4th | Partially blocked | A clean 4th barrier plausibly P-vs-NP-hard to formulate; Williams & GCT escape for orthogonal reasons |
| 2 | Williams algorithmic (ACC0→TC0) | **Dead (the trick)** / Partially blocked (program) | Threshold approx-degree Θ√t `[alman-williams-2015]` ⇒ provable n²-wire wall `[n2-wall]` |
| 3 | GCT (perm vs det) | Partially blocked (barrier-CLEAN, construction-LOCKED) | **Cycle 26:** escapes all THREE local barriers `[gct-escapes-three-barriers]` (the only surface that does) but the **flip** `[gct-flip-is-construction-lock]` IS `[witness-needs-explicit-lb]` (explicit short-witness construction); barriers LOCAL, construction UNIVERSAL `[barriers-and-construction-are-complementary]`; flip's Kronecker/plethysm objects NP-hard/#P-hard `[flip-construction-objects-are-nphard]`; VP≠VNP necessary-not-sufficient for P≠NP `[vnp-separation-not-pneqnp]`; positivity needs extending RH-over-finite-fields to nonstandard quantum groups (unknown) `[gct-flip-needs-deep-open-math]`. Plus the older structural bottleneck: determinant orbit boundary unknown n≥9; `[coarsening-gap]` + `[fixed-degree-escape]` open a crack |
| 4 | Independence / bounded arithmetic | **Dead for P vs NP** | `[meta-duality]`: independence ≡ direct LB, dualized — not a shortcut |
| 5 | Proof complexity (Res(⊕)) | Dead for feedback / **near-reachable** on frontier | No natural system conditionally p-optimal; Res(⊕) size-rank conversion missing `[resopl-obstruction]` |
| 6 | Descriptive complexity | **Dead** | Order-invariant LFP fails 0-1 law via universe parity `[shelah-1996]` |
| 7 | Communication / KW | Partially blocked | Full success only P≠NC¹; composition wall `[meir-2023]`; graph-cover route `[cavalar-oliveira-2025]` is the P≠NP-adjacent thread |
| 8 | Seiller dynamical | Blocked for P vs NP / **alive for L vs P** | P vs NP is semantic `[semantic-invariant-gap]`; dynamical invariants miss it |
| 9 | Meta-complexity / MCSP | **Alive** (deepened) | Not dies — open. MCSP `[mcsp-def]` exact complexity unknown; hardness de-conditioned to `[hirahara-ilango-2025]`. **Barrier status corrected `[barrier-evasion-overstated]`:** a *candidate* route, not barrier-immune — natural-proofs-caught in the easy direction (`[mcsp-easy-naturalproof]`), relocated via `[locality-barrier]` in the hard direction, relativization unproven, algebrization unexamined. **Honest gap `[goldilocks-structure]`:** near-term de-conditioning (quasi-poly/polytime) ⇒ EXP≠ZPP-class, **not P≠NP**; P≠NP needs route A (uniform-AC⁰) or crossing route B (locality barrier) `[route-a-route-b]` |

## Honest contract `[honest-ceiling]`
No angle produced, or came close to, a super-poly circuit LB for any natural class above ACC⁰. The frontier remains NEXP⊄ACC⁰ / Murray-Williams NTIME[n^polylog n]⊄P/poly. The value is a precise map + two novel diagnoses ([[novel-diagnoses]]) + one novel combined angle ([[algorithmic-gct]]) + the deepest hole ([[semantic-invariant-gap]]) + the biggest missed direction, now covered ([[mcsp-meta-complexity]], was [[missed-angles]]).

## The critic's corrected portfolio (not a single bet)
1. **Near-term / safe** — Res(⊕) size-rank conversion ([[proof-complexity]]). Rank LB exists; only conversion missing; obstruction diagnosed.
2. **Medium-term / headline** — Chen-2018 Max-IP shave `[chen-2018-maxip]` ⟹ NEXP ⊄ poly-size THR∘THR ([[williams-algorithmic]]). Proven-correct sufficient condition. **Pushed 2026-08-21 (maxip + majmaj):** the exact-Max-IP shave is partly SETH-blocked (`[maxip-shave-obstruction]`); the derandomization opening (SAT→CAPP, Chen-Williams `[chen-williams-2019]`) escapes SETH. **Correction `[majmaj-symmetry-deflated]`:** the *MAJ∘MAJ-vs-THR∘THR* distinction is illusory — Chen-Williams prove the two are **CAPP-equivalent**, so MAJ symmetry gives no PRG advantage; MAJ∘MAJ does not escape the `[n2-wall]`. Both natural PRG mechanisms are bounded away `[dilv-2024]`. The genuine constructive path is the **mining program** `[mining-program]` (NW reconstruction for threshold classes from the known HMPT lower bound `[hmpt-1993]`; payoff = NEXP ⊄ THR∘THR *and* TC⁰₃ via Chen-Ren `[chen-ren-2020]`), with Tell's quantified-derandomization wire-count `[tell-2018-quantified]` as the within-reach lever. **Mining seam verified 2026-08-21** (sources/2026-08-21-mining-program-seam.md): the *direct* NW-from-HMPT route is blocked at a **worst-case vs average-case** gap `[mining-worst-vs-average]` — **refined (second pass) to a quantitative+regime gap**: average-case correlation LBs for depth-2 threshold **exist** (CSS16 `[css-2018]`: ≤ n^{1+ε_d} wires, n^{−ε_d} correlation; Kane-Williams `[kane-williams-2016]`: superquadratic-wire) but are **few-wire / polynomial-correlation**; the exponential HMPT/Forster bounds are **worst-case SIZE** (discrepancy bounds correlation with *rectangles*, and a size-s MAJ∘THR circuit is richer than s rectangles, so at s=2^{Ω(n)} the correlation is not small). NW/Chen-Ren for a full-poly-size CAPP needs **exponential average-case-correlation** (2^{−Ω(ℓ)}) for full-poly-size MAJ∘MAJ — **open**. Amplification (IW XOR lemma) drifts the class to XOR∘MAJ∘MAJ (MAJ∘MAJ not closed under top XOR) + needs a well-structured hard function (Chen-Rothblum-Tell-Yogev JACM 2023) HMPT doesn't give; CAPP→LB is one-way, so an average-case MAJ∘MAJ LB is itself the open problem `[mining-nw-seam]`. **Tell's lever = the CSS16 wire frontier** — CSS16's average-case LB lives at the few-wire regime = Tell's promise regime, and Tell's open wire improvement is the *same question* as extending CSS16's correlation LB to more wires. **Cycle 2 (2026-08-23, sources/2026-08-23-wire-frontier-push.md): the gap is TWO-DIMENSIONAL** `[mining-gap-two-dimensional]` — correlation-strength (known: polynomial/subexponential exp(−n^{ε_d}); target: exponential 2^{−Ω(ℓ)}) × wire-regime (known: few-wire ≤ n^{1+ε_d}; target: full poly-size); CSS16 Parity is **wire-tight at n^{1.5}** (wrong function to push past n^{1.5}); the unrestricted THR∘THR worst-case exponential for IP₂ is itself **open** (Amano `[amano-2020]` / Kane-Williams Open Q #2), so **MAJ∘MAJ (HMPT worst-case anchor) is the only viable mining target**; Chen-Tal-Wang 2025 `[chen-tal-wang-2025]` (n^{2.5−ε} worst-case THR∘THR) goes the CAPP→LB direction (opposite of mining). **Cycle 3 (2026-08-23, sources/2026-08-23-nonamplification-route.md): the non-amplification route is CLOSED** `[non-amplification-also-class-drifts]` — STV `[stv-2001]` (PRGs without XOR lemma) and CRTY `[crty-2023]` (arithmetization wc→ac) both use arithmetic/parity decoder machinery exceeding MAJ∘MAJ (which lacks parity, HMPT `[hmpt-1993]`); the drift is **not XOR-lemma-specific** but lives in the reconstruction decoder (Goldreich-Rothblum `[goldreich-rothblum-2019]`: wc→ac works only for parity/arithmetic-containing classes like AC⁰[2], not MAJ∘MAJ). No new lever; the genuine-open shape reduces to a **direct** exponential average-case-correlation LB for MAJ∘MAJ, or a MAJ∘MAJ-decodable locally-list-decodable code (equivalent; none known). **Cycle 4 (2026-08-23, sources/2026-08-23-aggregation-crux.md): the obstruction is one level BELOW the decoder drift — at the amplification/aggregation step** `[threshold-aggregation-obstruction]` `[avg-worst-2x2-cell]`. A rough calc (agreement of a threshold of s low-discrepancy functions ≤ O(s·disc), disc(IP₂)=2^{−n/2}) appeared to give exponential average-case correlation at *poly* size — the mining target, contradicting the field; verified **closed**. HMPT decomposed: Lindsey (HMPT 3.4) is avg-case correlation at **depth 1 only**; the Discriminator Lemma (HMPT 3.3) is **worst-case**, lifting avg(C)→worst(MAJ∘C), never avg(MAJ∘C); CLLO21 `[cllo-2021]` converse (worst(MAJ∘C) ⟺ avg(C)) returns to avg(C), not avg(MAJ∘C). The mining target (avg(MAJ∘C), C=depth-1) is the **uncovered fourth cell** of the (avg/worst)×(C/MAJ∘C) grid `[avg-worst-2x2-cell]`. The linear bound fails because the **top threshold is a nonlinear aggregator**: boosting shows low per-gate correlation ≠ low majority correlation (CLLO21 SUM]-gate gap; amplification fails for non-majority-closed classes); Kumar CCC 2023 `[kumar-2023]` shows small-bias gates individually uncorrelated with parity yet aggregate to *compute* it; GHR92 (weight ~1/correlation) shows aggregation is limited but not prevented — so aggregate correlation can be high once s ≳ 1/disc ≈ 2^{n/2}, where HMPT's worst-case bound also kicks in. The exponential average-case bound at *poly* size does not follow. Fifth angle on `[witness-needs-explicit-lb]`; the genuine-open shape now has the obstruction's *mechanism* (defeat majority-aggregation / anti-boosting for IP₂), not merely its location. **The unification `[witness-needs-explicit-lb]` now spans BOTH live routes** (meta-complexity Route A + the Williams mining program): both need an explicit average-case / lower-bound-carrying witness for a restricted class, whose explicit construction IS the circuit-LB problem (natural-proofs frontier); the cheap inputs (worst-case LB / non-uniformity / randomness) supply it only insufficiently; the near-term tractable seam in both is a *relaxation* (structurally-different uniform-AC⁰ reduction / Tell's promise derandomization). No derandomization built; no lower bound proved; `[honest-ceiling]`.
3. **Long-term / highest upside** — Meta-complexity / MCSP ([[mcsp-meta-complexity]]). The one program where targets are circuit LBs / structural collapses and conditional results are being de-conditioned. The clear second wave. **Pushed 2026-08-21 (Route A):** the **only direct P≠NP bridge** is Route A — NP-hardness of MCSP under **logtime-uniform AC⁰ many-one reductions** ⟹ NP ⊄ P/poly ⟹ P≠NP (Murray-Williams Thm 1.8 `[murray-williams-2017]`). Sharpened: the resistance is **structural** — the **compression crux** `[route-a-compression-crux]` (encode `n`-bit SAT into the circuit complexity of an `O(log n)`-bit function) + **dual locality** `[route-a-dual-locality]` `[choprs-2020]` (the locality barrier blocks route B AND touches Route A's gap construction) — **not cryptographic** `[route-a-owf-escape]` (uniform-AC⁰ sits below the OWF obstruction and the Mazor-Pass Levin negative). **Variant gap** `[route-a-variant-gap]`: no MCSP variant is known NP-hard under uniform-AC⁰ many-one reductions; the closest is Ilango's `(AC⁰_d)-MCSP` `[ilango-2020]` (randomized + quasipoly + Turing + restricted). Full construction = the breakthrough itself; tractable sub-targets S1–S3. **S1 attacked 2026-08-21** (sources/2026-08-21-s1-push.md): the closest, most tractable sub-target — derandomize Ilango's `(AC⁰_d)-MCSP` randomized quasipoly-time Turing reduction `[ilango-2020]` to deterministic. **Precedent** `[s1-hos18-precedent]` `[hos-2018]`: HOS18 *already* derandomized `(DNF◦MOD_m)-MCSP` to deterministic polytime (Naor–Naor ε-biased distributions) — derandomizing restricted-class MCSP reductions is achievable in principle; S1 extends it to the harder `(AC⁰_d)-formula` class (open). **Named plausible mechanism** `[s1-chebyshev-moce-oracle]`: the Chebyshev/second-moment Splitting Claim only needs pairwise independence (short enumerable seed), and in a *Turing* reduction the method-of-conditional-expectations can use the *oracle* to evaluate the circuit-complexity potential (inductive d-1↔d structure) — *not completed*, linchpin flagged. **Obstruction** `[s1-meta-vs-lowdegree]`: ε-biased fools low-degree/parity tests, not meta-computational conditions (same flavor as `[dilv-2024]`). **S1.a verified against primary source 2026-08-21 (self-correction, see sources/2026-08-21-s1a-primary-source.md):** the Chebyshev-MOCE mechanism was a **phantom** `[s1a-chebyshev-phantom]` — the Splitting Claim (Claim 21) is proof-internal to the lifting theorem (Theorem 5), used only as a deterministic inequality in the correctness proof; the reduction algorithm (ECCC TR20-183 lines 2446-2474) never runs it, so the "linchpin" is answered NO (mis-aimed) and the "simultaneity obstruction" dissolves (it lives in the probabilistic-method proof, not the algorithm). The **sole real randomness is the Lupanov sampling of g** `[s1a-lupanov-only-rng]` (Lemma 26): a small-seed distribution (t·m≈2ⁿ·n²/δ enumerable coins), asymmetric — upper bound constructive/free, lower bound = derandomized union bound over a 2^{O(t·n²·log n)}-sized formula family = the obstruction **located exactly** `[s1a-antiprox-obstruction]` (sharpened `[s1-meta-vs-lowdegree]`). **Re-rated (honest):** the genuine S1 core is a **fine-grained DNF lower bound for an explicit function** in a tight window — circuit-LB-hard; the "most tractable Route-A-adjacent step" framing is tempered (the derandomization is not obviously closer than a fine-grained circuit LB). **Honest bound** `[s1-necessary-insufficient]`: a full S1 closes only the randomized→deterministic axis — Route A still needs quasipoly→AC⁰ + Turing→many-one + restricted→original; necessary-but-insufficient for P≠NP. **Honest caveat:** near-term de-conditioning (`[hirahara-ilango-2025]`) yields EXP≠ZPP-class, not P≠NP; barrier-evasion is *candidate*, not proven (`[barrier-evasion-overstated]`); the two formal P≠NP bridges `[route-a-route-b]` are both currently blocked. **S2 analyzed against the primary source 2026-08-21** (sources/2026-08-21-s2-primary-source.md — verified NEGATIVE result): MOCSP reaches *non-uniform* AC⁰ many-one (Ilango ITCS 2020), so `[route-a-variant-gap]` is precisely **non-uniform→uniform AC⁰ many-one**. The non-uniformity hardwires an (rn)-irritable T (random T irritable w.h.p.); uniformizing needs an **explicit exponential (in L=O(log n)) general-circuit lower bound** (>2^{Θ(L)}; best known explicit Θ(L), linear — exponentially short) `[s2-irritable-witness-obstruction]` = the natural-proofs frontier. **Unifying** `[witness-needs-explicit-lb]`: S1.a (DNF witness, tight window) + S2 (general-circuit witness, exponential LB) = two faces of one obstruction — derandomizing/uniformizing meta-complexity AC⁰ reductions needs an explicitly-constructed lower-bound-carrying witness, which *is* the circuit-LB problem; non-uniformity/randomness is the only known way to obtain one. S2 is the harder face (already at the explicit-exponential-circuit-LB frontier *before* the full restricted→original gap). **S3 analyzed against the primary source 2026-08-23** (sources/2026-08-23-s3-choprs-thm49.md — verified NEGATIVE): CHOPRS Thm 49 is a *non-localizable* LB for an Andreev-like function L ∈ E (formula world, *not* AC⁰, *not* MCSP), proving the `[locality-barrier]` `[choprs-2020]` is not absolute — but the escape is purchased with an *existential* hard function f_hard (exhaustive search), needing an **explicit exponential (in m=log N) formula LB** (best explicit Ω̃(m³) cubic, needed 2^{Θ(m)}) `[s3-locality-escapes-witness-barrier-does-not]`. **Barrier orthogonality** `[locality-vs-witness-orthogonal]`: the locality barrier and the `[witness-needs-explicit-lb]` barrier are distinct — escaping locality (Thm 49) ≠ escaping the witness barrier; the non-localizability is bought with non-uniformity, the same existential pattern as S2's irritable T. The `[witness-needs-explicit-lb]` diagnosis now spans **three circuit models**: S1.a = DNF (explicit exponential DNF LBs *exist*; crux = tight window), S2 = general circuits (best explicit Θ(L) linear, needed 2^{Θ(L)}), S3 = formulas (best explicit m³ cubic, needed 2^{Θ(m)}) — all three need an explicit exponential (in L=O(log n)) LB for a function on O(log n) bits, all stuck at polynomial best-explicit. **Honest scope:** rules out porting *this* Thm 49 escape via explicit f_hard; not a non-localizable LB whose hard function *is* explicit (none known); not P≠NP regardless. No P≠NP claimed; `[honest-ceiling]`.

## Five-cycle synthesis (2026-08-23) — the frontier is one wall, blocked by an open construction `[five-cycle-synthesis]`
The 5-cycle loop (Cycles 1–4 adaptive pushes + this synthesis) confirms the `[witness-needs-explicit-lb]` obstruction from **seven angles** spanning **both** live P≠NP-adjacent routes and **three circuit models** (DNF / general-circuit / formula) plus the depth-2 threshold average-case world: S1.a (DNF, tight window), S2 (general-circuit, explicit-exponential), S3 (formula, explicit-exponential + barrier orthogonality), Cycle 2 (two-dimensional wire gap), Cycle 3 (decoder-level class drift, closed), Cycle 4 (amplification/aggregation level — the top threshold is a nonlinear aggregator). Every angle converges on: *both routes require an explicitly constructed lower-bound-carrying / average-case-hard witness for a restricted class; a cheap probabilistic/existential input supplies it only insufficiently; the explicit construction IS the circuit-lower-bound problem (natural-proofs frontier).*

**Untried-lever analysis (the final adaptive consideration):** every remaining lever across the two routes either hits the same wall or *is itself* the breakthrough (no concrete candidate): (1) direct worst-case→CAPP without average-case = the SAT route, blocked by the **proven** `[n2-wall]`; (2) a majority-closed MAJ∘MAJ subclass — near-contradictory (Cycle 4's point); (3) a non-IP₂ function with a known exponential avg-case LB vs full-poly-size MAJ∘MAJ = *the* open problem; (4) a structurally-different uniform-AC⁰ MCSP reduction = the compression crux (≥ as hard as NP ⊄ P/poly). The one genuinely different untried lever (Res(⊕) size-rank conversion, `[resopl-obstruction]`) is a *separate problem* (proof complexity, NP≠coNP-adjacent) with its own wall.

**The asymmetry that matters:** the worst-case SAT route is blocked by a **proven theorem** (`[n2-wall]`); the average-case CAPP (mining) route is blocked by an **open construction** (explicit exponential avg-case-correlation LB). The avg-case/mining route is therefore the genuinely *more-promising* Williams-side sub-route — blocked by open-ness, not a theorem; a single explicit construction falls it. This sharpens the survey's "constructive path" framing: constructive *because* its obstruction is an open construction rather than a proven impossibility.

**Honest assessment:**
- The frontier is **one wall, two routes**, governed by `[witness-needs-explicit-lb]`.
- The wall is an **open problem, not a proven impossibility** (the natural-proofs barrier behind it is *conditional* on one-way functions). The frontier is **genuinely alive, at the hardest known point** — not closed by a theorem.
- **Actionable conclusion:** a single idea producing an explicit average-case / LB-carrying witness for ANY ONE restricted class (in derandomization-usable form) advances BOTH routes. The two near-term *relaxations* remain the tractable seams: (meta-complexity) a structurally-different uniform-AC⁰ reduction; (Williams) Tell's promise/wire-count derandomization = the CSS16 wire frontier.
- **Near-term ≠ P≠NP** (unchanged): near-term de-conditioning ⟹ EXP≠ZPP-class. P≠NP still requires crossing the wall.
- **Deliverable is a map, not a theorem.** No P≠NP proof, no new circuit LB, no derandomization was produced. The product is a precise, falsifiable map — seven angles on one wall, the wall's *mechanism* identified (threshold nonlinearity / aggregation; the existential-witness pattern), the wall's *status* honestly classified (open construction), and the two routes unified so a single success propagates. See sources/2026-08-23-five-cycle-synthesis.md for the full synthesis.

## Second loop — Cycle 1 (2026-08-23): the AC⁰ face is the thinnest; the wall's blocking gate located exactly `[second-loop-cycle-1]`
The user's **second** 5-cycle mandate ("make a true breakthrough or solve the problem") attacks the wall itself rather than a lever. Cycle 1 asks which *face* of the `[witness-needs-explicit-lb]` wall is thinnest and pushes there — and **corrects an over-broad claim of the first-loop synthesis** `[parameterization-correction]`. The synthesis (above, and the S3 line) states "all three circuit models stuck at **polynomial** best-explicit." This is **wrong for the AC⁰ (S1.a) model**: explicit exponential AC⁰ LBs **exist** `[ac0-lb-existence-passed]` `[sipser-rst-2015]` — Sipser_d (Håstad 1986 worst-case; Rossman-Servedio-Tan FOCS 2015 average-case) is computed by a *linear-size depth-d formula* while any depth-(d-1) circuit *approximating* it needs exp(n^{Ω(1/d)}), an explicit average-case-hard function with a known small upper bound. The witness barrier's "exponential in L" target has **different parameterizations per sub-target** (conflated by the synthesis): S2 (general circuit, on L=O(log n) bits — LB-existence **blocked**, best explicit Θ(L)) and S3 (formula, on m=O(log N) bits — **blocked**, best explicit m³), but **S1.a** (g on N=Θ(n²) bits, window up to 2^{Θ(√N)}; Sipser gives 2^{N^{Ω(1/(d-1))}} ≈ 2^{√N} for d=3 — in the right ballpark) — LB-existence **passed**. So the AC⁰ face is the thinnest.

**Why the existing LB doesn't fall the wall** `[ppoly-recognizable]` `[ac0-natural-proofs-wall]` (verified verbatim against Ilango FOCS 2020 §1.4, `_tr20-183.txt` lines 40-46/565-591/620-629): the `(AC⁰_d)`-MCSP reduction's correctness needs an LB collection that is **NOT (P/poly)-recognizable** (else algorithmizing it yields SAT∈subexp-circuits — a *collapse*, the *opposite* of Route A's separation); "many lower bound methods we know, like Håstad's switching lemma, yield (P/poly)-recognizable collections" and are ruled out. The known explicit exponential AC⁰ LBs are **natural** ⇒ would collapse SAT, not separate it. **Route A needs a non-natural explicit exponential AC⁰ LB = the natural-proofs frontier, located exactly in the AC⁰ model.** The S1.a obstruction is now **two partly-independent open gates** `[ac0-tight-window]`: (1) the natural-proofs/(P/poly)-recognizability gate (method recognizability) AND (2) the tight-window/two-sided-precision gate `[s1a-antiprox-obstruction]` (LB must pin g's complexity in [(1−4δ)T,(1+4δ)T] for each T; Sipser/RST are all-hard, fail the upper-bound side; no known tight-size-at-fixed-depth AC⁰ function).

**Net:** the wall stands; the map is sharper. Unification preserved + sharpened — `[witness-needs-explicit-lb]` still governs all three faces, but the blocking gate now differs: LB-existence (S2/S3) vs {non-naturality, tight-window} (S1.a). The AC⁰ face is one gate closer but blocked at the natural-proofs gate (the canonical barrier, where one would expect the wall hardest, since natural proofs *work* for AC⁰ — so the non-natural LB Route A needs is outside the standard toolbox). No breakthrough; the product is a primary-source-verified refinement locating the natural-proofs frontier exactly in the AC⁰ model. `[honest-ceiling]` See sources/2026-08-23-ac0-escape-hatch.md.

## Second loop — Cycle 2 (2026-08-23): Ilango's barrier is strictly stronger than natural proofs; the canonical non-natural escape is the wrong escape `[second-loop-cycle-2]`
Cycle 1 located the AC⁰ face's gate as the (P/poly)-recognizability gate. Cycle 2 asks the breakthrough question: **is there any known *non-natural* LB that crosses it?** The obvious candidate is Williams' algorithmic method (NEXP ⊄ ACC⁰), the canonical LB held to *evade* the natural-proofs barrier. **Answer: no — and the reason reveals the gate is stronger than Razborov-Rudich.** `[recognizable-stronger-than-natural]` `[largeness-vs-constructivity]`

- **Largeness vs constructivity are different axes; Ilango's gate is the constructivity axis.** Razborov-Rudich needs constructivity ∧ **largeness** ∧ usefulness; the standard escape (Williams') drops **largeness**. Ilango's (P/poly)-recognizable condition (TR20-183 §1.4) needs only constructivity ∧ usefulness — **no largeness requirement**. So Ilango's barrier is **strictly stronger** than RR for the MCSP-reduction-witness setting: it has no largeness condition to drop, and dropping largeness leaves constructivity intact, which is exactly what the gate forbids.
- **Williams' LB is still (P/poly)-recognizable** `[williams-constructive-theorem]`. Williams, "Natural Proofs vs. Derandomization" (SIAM J. Comput. 2016, Theorem 1.1): for typical C (incl. ACC⁰), NEXP ⊄ C ⟺ a P-time useful property against C **with O(log n) advice**; constructivity is **unavoidable** for NEXP LBs, and NEXP ⊄ ACC⁰ is non-natural by lacking **largeness**, not constructivity. "O(log n) advice" = P/poly-computable ⇒ Williams' LB **is (P/poly)-recognizable**. Used as the reduction's witness, it would yield SAT ∈ subexp-circuits (a *collapse*) ⇒ **ruled out**, exactly like the switching-lemma LBs. To cross the AC⁰ face's gate one must drop *constructivity* — produce a genuinely non-constructive LB.
- **The wall is a triple conjunction; non-recognizable ↔ non-constructive is in tension** `[triple-conjunction-wall]`. The S1.a witness must satisfy {non-(P/poly)-recognizable ∧ AC⁰-constructible explicit g ∧ tight-window} simultaneously. No known method achieves even the first two — switching lemma {✗-recognizable, ✓-constructible, ✓-tight-ish}, Williams {✗-recognizable (Thm 1.1), ✗-NEXP-not-constructible, ✗-coarse}, counting {✓-non-recognizable, ✗-no-explicit-f, ✗-coarse}. Gate 1 (non-recognizable) and constructibility are in **near-definitional tension**: non-constructive methods (counting) give no explicit witness; constructive methods (switching lemma, Williams) are recognizable ⇒ ruled out. The witness needs a method simultaneously non-constructive *and* constructive-of-an-explicit-g.

**Net:** the wall stands, now one level deeper and with its inner tension named. The genuine-open shape is maximally precise: **an explicit, AC⁰-constructible function g with a non-(P/poly)-recognizable tight-window AC⁰ LB**, where whether "non-recognizable" and "explicit-constructible" are even simultaneously achievable is itself **open** (Thm 1.1 makes them equivalent for NEXP, hinting at tension, but the explicit-f regime the S1.a witness lives in is uncovered). No breakthrough; `[honest-ceiling]` — flagged: Thm 1.1 is web-confirmed (search-summary-level, not PDF-line-verified); the non-recognizable↔non-constructive tension is a heuristic (sound for NEXP per Thm 1.1, unproven for explicit-f), not a theorem. See sources/2026-08-23-recognizable-stronger-than-natural.md.

## Second loop — Cycle 3 (2026-08-23): the tension is empirical, not definitional; the constructivity-forcing is NEXP-specific `[second-loop-cycle-3]`
Cycle 2 called the non-recognizable↔constructible tension "near-definitional." Cycle 3 tests that against the literature and **corrects it downward** — the tension is **empirical, not logical** `[tension-not-definitional]`. The one theorem that would make constructivity unavoidable — Williams Theorem 1.1 — does so **only for NEXP-class** LBs, via the **Easy Witness Lemma** (Thm 3.1, on IKW 2002: NEXP ⊆ P/poly ⟹ easy witnesses) `[easy-witness-mechanism]` `[constructivity-forcing-nexp-specific]`. The mechanism is the **nondeterministic-witness structure** (a nondeterministic class with small circuits has its witnesses as small circuits) — **absent** for explicit/polytime functions. The S1.a witness is an explicit (AC⁰-constructible) function, not a nondeterministic class, so the constructivity-forcing does **not** reach it: a non-recognizable explicit-f LB is NOT blocked by Williams' theorem. Corroborated: CWY ITCS 2023 `[cwy-2023-blackbox]` (black-box constructivity unavoidable for NEXP; **Thm 1.6: no deterministic polylog(N)-constructive property useful against AC⁰** — randomness essential, so the AC⁰ constructivity axis is delicate), Dhayal-Impagliazzo 2020 `[dhayal-impagliazzo-2020-unique]` (UEXP forcing via unique anti-largeness properties; retains constructivity; stays nondeterministic-class-level). **"Explicit g ⇒ recognizable" is false** `[recognizer-needs-mcsp]`: Ilango's recognizer must reject ALL easy functions (= MCSP-complement, the hard part, independent of g's explicitness); accepting g_n is easy, rejecting all easy is not — so an explicit g does not automatically trip the barrier, and a non-recognizable LB is compatible with explicit g.

**Net:** the wall is **reclassified, not removed** `[wall-loosened-not-fallen]` — from Cycle 2's "near-paradox (non-constructive yet explicit)" to "requires a genuinely new LB method certifying a specific explicit function hard WITHOUT a P/poly-checkable separator from the easy functions." Still hard (no such method known; Gate 2 tight-window also open) but **not ruled out by the constructivity-forcing theorem** (its mechanism is absent in the explicit-f regime). The genuine breakthrough shape (a non-recognizable explicit-f tight-window AC⁰ LB) is now certified possible-in-principle, while remaining not achieved. No breakthrough; `[honest-ceiling]` — flagged: Williams/CWY/Dhayal-Impagliazzo web-confirmed (search/arXiv-summary-level, not PDF-line-verified); the "loosening" is a more-accurate *framing* correction (Cycle 2's "near-definitional" was too strong), NOT a loosening of the wall itself; the boundary case (an explicit g also in a nondeterministic class, e.g. NP∩coNP) is unaddressed. See sources/2026-08-23-tension-not-definitional.md.

## Second loop — Cycle 4 (2026-08-23): the boundary case — g ∈ NEXP, so the S1.a LB entails the class LB; the wall pins to Williams' (=>) targetability `[second-loop-cycle-4]`
Cycle 3 (above) reclassified the wall as "requires a genuinely new LB method" and left one caveat open in its honest-scope: an explicit g **also** in a nondeterministic class. Cycle 4 (sources/2026-08-23-boundary-case-targetability.md) observes the S1.a witness **is** such a function — g is AC⁰-constructible ⊆ P ⊆ NP ⊆ NEXP — and tests whether this re-tightens the wall.

- **The single-function LB entails the class LB** `[single-lb-implies-class-lb]`. Since g ∈ NEXP and g is hard for size-T·AC⁰_{d−1}, g is an NEXP *witness of non-containment*: the S1.a LB implies NEXP ⊄ size-T·AC⁰_{d−1}. Purely logical (a single hard NEXP function is exactly what non-containment means). The S1.a LB sits *inside* the class-LB regime Williams' theorem governs, even though its *proof mechanism* (explicit-f) is outside the easy-witness forcing (Cycle 3).
- **A recognizable property is guaranteed in the vicinity** `[williams-iff-vicinity]`. Williams Thm 1.1 (⇒) is an IFF (class LB ⟺ recognizable useful property), so **whenever the S1.a witness exists, a (P/poly)-recognizable useful property against size-T·AC⁰_{d−1} EXISTS.** This partially walks back Cycle 3's "no theorem forces recognizability here": the forcing's *consequence* (a recognizable property exists) is present even where its *mechanism* (easy-witness diagonalization) is absent.
- **The wall pins to (Q-targetability)** `[targetability-circularity]` `[wall-pinned-to-targetability]`. The reduction needs the *specific* g (AC⁰-constructible, tight-window), not "some" hard NEXP function h. So the wall turns on whether Williams' (⇒) constructs a recognizable property accepting the *specific* g, or only a generic h ≠ g. If targetable onto g ⟹ re-tightening (collapse ⟹ Cycle 3 overturned); if only generic h ⟹ stays loosened (P* accepts a different function, unusable in the reduction). My flagged reasoning favors "generic h only" via a **circularity**: retargeting onto g requires g's LB, but a recognizable g-LB already collapses (Ilango) and a non-recognizable one cannot be the retargeting certificate.

**Net:** the boundary case Cycle 3 left open is now **reduced to a precise open question** (Williams' (⇒) targetability / constructive content), not resolved. The wall lands **between** Cycle 2 (near-definitional) and Cycle 3 (loosened), pinned to a single named sub-question: *soft iff Williams' guaranteed recognizable property cannot be retargeted onto the specific g; hard iff it can.* No breakthrough; `[honest-ceiling]` — the (A)+(B) chain is solid (g ∈ NEXP, class-LB entailment, Williams IFF) but the targetability/circularity claim is reasoning from the search-level theorem statement, NOT PDF-line-verified; if Williams' (⇒) permits arbitrary retargeting onto any given hard f, the circularity dissolves and the wall re-tightens (resolving this needs Williams 2016 §3, not done here). A d-dependent threshold subtlety is also flagged (whether NEXP ⊄ size-T·AC⁰_{d−1} at the Lupanov window T is itself known vs open). See sources/2026-08-23-boundary-case-targetability.md.

## Second 5-cycle loop synthesis (2026-08-23) — the AC⁰ face mapped; the wall pinned, not moved `[second-loop-synthesis]` `[wall-pinned-not-moved]`
The second loop (Cycles 1–4 + this synthesis) attacked the wall's **thinnest face directly** — the AC⁰ (S1.a) face, where LB-existence PASSES (Cycle 1) — and traced the recognizability gate (Gate 1) across four `[honest-ceiling]`-driven self-correcting cycles:

- **Cycle 1** `[ac0-lb-existence-passed]` `[ppoly-recognizable]` `[ac0-tight-window]`: explicit exponential AC⁰ LBs **exist** (Sipser/RST) so LB-existence PASSES — the AC⁰ face is the thinnest. But the known LBs are `(P/poly)`-recognizable (Ilango §1.4, line-verified) ⇒ would collapse SAT. Wall = two gates: recognizability + tight-window.
- **Cycle 2** `[recognizable-stronger-than-natural]` `[triple-conjunction-wall]`: Ilango's gate is the **constructivity** axis (not largeness) — **strictly stronger than Razborov-Rudich**; the canonical non-natural escape (Williams NEXP⊄ACC⁰, drops largeness) is the WRONG escape. Wall = triple conjunction {non-recognizable ∧ AC⁰-constructible explicit g ∧ tight-window}; non-recognizable↔constructible in **near-definitional** tension. *(most pessimistic)*
- **Cycle 3** `[tension-not-definitional]` `[wall-loosened-not-fallen]` `[recognizer-needs-mcsp]`: corrects Cycle 2 DOWNWARD — the constructivity-forcing (Williams Thm 1.1 via the Easy Witness Lemma) is **NEXP-specific**, absent for explicit f; "explicit g ⇒ recognizable" is false (recognizer must reject ALL easy = MCSP-complement). Wall reclassified from near-paradox to **requires a genuinely new LB method** — possible-in-principle, NOT theorem-blocked. *(most optimistic)*
- **Cycle 4** `[single-lb-implies-class-lb]` `[williams-iff-vicinity]` `[wall-pinned-to-targetability]` `[targetability-circularity]`: closes Cycle 3's flagged caveat — g IS in NEXP (AC⁰-constructible ⊆ P ⊆ NEXP), so the single-function LB **entails** the class LB NEXP ⊄ size-T·AC⁰, and Williams' IFF (⇒) **guarantees a recognizable property EXISTS in the vicinity**. Partially walks back Cycle 3 (forcing's *consequence* present where *mechanism* absent). Wall now pins to **(Q-targetability)**: does Williams' (⇒) accept the *specific* g or only a generic h? Flagged reasoning (retargeting onto g is circular — needs g's LB; a recognizable one collapses, a non-recognizable one can't retarget) favors generic-only. *(medium-low)*

**The trajectory is not monotone** — Cycle 2 most pessimistic (near-definitional, possibly impossible), Cycle 3 most optimistic (empirical, possible-in-principle), Cycle 4 recovered *some* pessimism (vicinity-recognizability guaranteed) but not all (the property is for a generic h, not necessarily g). **The net is a pinning, not a fall and not a proof of impossibility.**

**Did the wall move?** `[wall-pinned-not-moved]` — **No breakthrough** (no P≠NP proof, no new circuit LB, no derandomization, no explicit witness). But the wall's *locus* moved from vague → exactly-located → harder-than-natural-proofs → empirical-not-definitional → pinned-to-one-question. The AC⁰ face's two gates are now exactly characterized; the breakthrough shape (a non-recognizable explicit-f tight-window AC⁰ LB) is certified **possible-in-principle** (Cycle 3) **but now conditioned** (Cycle 4) on Williams' (⇒) not being retargetable onto g. The net movement is in PRECISION and CONDITIONALITY, not in the wall itself.

**The loop's actionable product — one concrete next verification:** across four cycles the entire outcome converges to a single, falsifiable, line-verifiable question — **(Q-targetability)**: is Williams' Theorem 1.1 (⇒) (class LB ⟹ recognizable useful property) retargetable onto a *specific chosen* hard function f, or does it produce a property only for an *unspecified/generic* NEXP hard function h? Resolve by reading Williams 2016 §3. If retargetable onto g ⟹ re-tightening (Ilango collapse ⟹ Cycle 3 overturned; AC⁰ face blocked at Gate 1 by a now-understood theorem). If generic only ⟹ stays loosened (g's non-recognizable LB survives; AC⁰ face's breakthrough shape remains possible-in-principle, blocked by the empirical non-recognizable-method gap + Gate 2). Either answer sharpens the map.

**Two-loop arc:** the first loop confirmed `[witness-needs-explicit-lb]` from seven angles across both routes + three models (the wall is an open construction, not a proven impossibility; the proven-theorem SAT route vs the open-construction mining route). The second loop attacked the thinnest face and drove the recognizability gate's status to a single checkable question. **The wall neither fell nor was proven impossible; it was driven from vague to precisely pinned.** Near-term ≠ P≠NP (unchanged: ⟹ EXP≠ZPP-class). The two near-term *relaxations* remain the tractable seams (a structurally-different uniform-AC⁰ reduction; Tell's promise/wire-count derandomization = the CSS16 wire frontier). See sources/2026-08-23-second-loop-synthesis.md. `[honest-ceiling]` — the loop's pivotal claim ((Q-targetability) + the circularity reasoning) is FLAGGED, not PDF-line-verified; the d-dependent threshold subtlety (whether NEXP ⊄ size-T·AC⁰_{d−1} at the Lupanov window T is known vs open) is flagged.

## Third loop — Cycle 6 (2026-08-23): (Q-targetability) RESOLVED — Williams' (⇒) accepts a structurally-determined function, not an arbitrary g; Gate 1 resolved soft; Cycle 3 un-conditioned `[third-loop-cycle-1]` `[q-targetability-resolved]`
The second loop's synthesis named a single actionable next verification — **(Q-targetability)**: is Williams' Thm 1.1 (⇒) (class LB ⟹ recognizable useful property) retargetable onto a *specific* chosen hard f, or only a *generic* NEXP hard h? Cycle 4 (above) had flagged this as the locus on which the wall's status turns (soft iff generic; hard iff targetable onto g) and reasoned from the theorem *statement* that "generic" was likely via a circularity. Cycle 6 (sources/2026-08-23-q-targetability-resolved.md) obtains the actual (⇒) **construction** (Williams 2016 Thm 3.2/3.3) and resolves it:

- **The (⇒) construction.** The recognizable property is **P(f)=1 iff V(x'ᵢ, f) accepts**, where V is a "good" NEXP verifier (TIME[2^{O(n)}]) and x'ᵢ is an existentially-found "bad" input (every accepting witness of V(x'ᵢ,·) has C-complexity > s), specified to the algorithm only by an O(log n) index as advice (Thm 3.2). The universal version (Thm 3.3, via SUCCINCT HALTING) accepts an **accepting computation history of an NTM**. The accepted function is **structurally determined** (an accepting witness / computation history), NOT an arbitrarily chosen hard function. `[williams-iff-non-targetable]`
- **Retargeting onto g is circular — now source-grounded.** `[retargeting-circular-confirmed]` Targeting the specific S1.a witness g requires engineering V so g's truth table is the accepting witness of a *bad* input. Certifying "bad" requires *proving* every accepting witness (incl. g) has high C-complexity = **g's LB**. A recognizable g-LB makes the engineered property recognizable ⟹ Ilango collapse; a non-recognizable g-LB cannot be the badness certificate. Circular — and this is now the *construction's structure* (the bad-input certificate IS a high-complexity LB), not just the theorem-statement reasoning of Cycle 4.
- **Different objects; the vicinity-recognizability does NOT re-tighten.** `[vicinity-does-not-retighten]` Williams' (⇒) function is one-sided-hard (no tight window) and not AC⁰-constructible (an NEXP computation). The reduction's witness g must be AC⁰-constructible *and* tight-window. The recognizable property Williams guarantees (for h) and the reduction's specific witness (g) are **different objects** — existence-of-a-useful-property ⟂ construct-a-specific-witness = the `[witness-needs-explicit-lb]` gap. The Ilango collapse is triggered by the *reduction's witness's* LB being recognizable, not by any nearby recognizable property; a recognizable property for h certifies nothing about g's window and cannot substitute in the reduction's correctness proof. So the vicinity-recognizability Cycle 4 found does **not** re-tighten the wall.
- **Net — Gate 1 resolved soft; Cycle 3 un-conditioned.** `[wall-stays-loosened]` `[q-targetability-resolved]` Cycle 3's "possible-in-principle" is now **un-conditioned**: the breakthrough shape (a non-recognizable explicit-f tight-window AC⁰ LB) is certified possible-in-principle, blocked *only* by Gate 2 (tight-window) and the empirical non-recognizable-method gap — NOT by the constructivity-forcing theorem, whose guaranteed recognizable property provably lives at the wrong function. **Gate 1 (recognizability) is now fully resolved as soft**; the third loop's leverage shifts to Gate 2 + the method-gap. Red herring noted: the one-sidedness of Williams' (⇒) function is NOT an independent blocker of re-tightening (if g were targetable, accepting g + rejecting easy ⇒ collapse regardless) — re-tightening is blocked SOLELY by non-targetability.

**Did the wall move?** No breakthrough (no P≠NP proof, no new circuit LB, no derandomization, no explicit witness). But the second loop's pivotal *flagged* question is now **resolved** (generic h only, not targetable onto g), and the conclusion is **strengthened** — from Cycle 4's "possible-in-principle, *conditioned* on (Q-targetability)=generic" to Cycle 6's "possible-in-principle, *un-conditioned*." The recognizability gate (Gate 1) — the locus the entire second loop traced — is now **fully resolved as soft**. The third loop's remaining leverage is Gate 2 (tight-window) and the empirical non-recognizable-method gap, both located exactly. `[honest-ceiling]` — source-grounded (Williams 2016 §3 proof structure via search: the bad-input x'ᵢ, the V-witness property P with O(log n) advice, the universal HISTORY/accepting-computation-history) but NOT PDF-line-verified; the targetability conclusion follows directly from the construction's existential wording and is robust to its details; a clever-encoding subtlety is flagged but the conclusion is robust. See sources/2026-08-23-q-targetability-resolved.md.

## Third loop — Cycle 7 (2026-08-23): the two-gate wall is OVERCOUNTED — Gate 1 is automatic for the Lupanov method; the wall collapses to (A) derandomization + (B) a naturalization question `[third-loop-cycle-2]` `[two-gates-overcounted]`
Cycle 6 (above) resolved (Q-targetability) and discharged Gate 1 as soft from one direction (Williams' (⇒) is structurally non-targetable onto the specific g). Cycle 7 (sources/2026-08-23-two-gates-overcounted.md) re-reads Ilango §1.4 (`_tr20-183.txt`, content-lines 559–629, read verbatim) and finds the second-loop "two **partly-independent** gates" framing (Cycle 1) **overcounts**: the two gates are not independent binding constraints — a single method plausibly crosses both simultaneously.

- **Finding 1 — Gate 1 is automatic for any successful deterministic many-one reduction** `[gate-1-automatic]`. Ilango's theorem (lines 559–564): a *deterministic many-one* (C)-MCSP reduction using an LB collection S implies recognizing S is **coNP-hard** ⇒ under coNP ⊄ P/poly, S is **NOT (P/poly)-recognizable** (lines 565–572 define recognizable; "if one believes coNP ⊄ P/poly, it better not be the case that S can be computed in P/poly"). So Gate 1 (non-recognizability) is **not a separate binding gate** a method must additionally cross — it is a **filter on the proof method**: any reduction that *succeeds* automatically uses a non-recognizable collection (else it would not succeed). The conditional (lines 630–631) is "deterministic many-one"; the existing reduction is randomized + Turing, so the filter is not yet triggered — but it shows Gate 1 is a *consequence* of success, not an independent obstacle.
- **Finding 2 — Ilango's own Lupanov/direct-sum method is the candidate non-recognizable method, AND it achieves the tight window** `[lupanov-plausibly-nonnatural]` `[lupanov-largeness-violated]`. Ilango's own discussion (lines 601–619) of his Theorem 2 (the depth-gap via direct-sum/lifting with Lupanov-sampled g — *the S1.a witness method*) flags it on **both** natural-proofs axes: (a) **largeness violated** — Lupanov gives L_d(f)=(1+o(1))L_{d+1}(f) for d≥3 w.h.p., so random functions have *no* depth-gap; the tight-window/gap property is a **negligible-fraction** property over uniform functions ⇒ not large; (b) **constructivity plausibly violated** — "not clear how to recognize the functions witnessing this lower bound in polynomial time given a truth table." His caveat (lines 615, 620–625): "does not mean it does not naturalize, just that it does not obviously naturalize"; a non-recognizable S might still have a (P/poly)-recognizable variation S' (Smolensky-style). His own open question (line 615): "it would be interesting to explore whether one can put this result in the framework of Natural Proofs." So the very method that achieves Gate 2 (the tight window) is itself the candidate non-recognizable method for Gate 1.
- **Finding 3 — the two-gate wall collapses to derandomization + one naturalization question** `[wall-collapses-to-derandomization]`. A single method (Lupanov/direct-sum) plausibly crosses BOTH gates at once (existentially, w.h.p.): it achieves the tight window (Gate 2) and is flagged non-large / not-obviously-recognizable (Gate 1). The wall is no longer "two independent gate-crossings" but two tasks: **(A)** derandomize the Lupanov sampler (= the `[s1a-antiprox-obstruction]` = a fine-grained DNF LB for an explicit function in the tight window = the circuit-LB frontier = S1.b′) + **(B)** prove the Lupanov/direct-sum LB is genuinely non-naturalizing (Ilango's own open question). One derandomization + one naturalization proof, not two independent gate crossings.
- **The ONE residual Gate-1 concern — the naturalization question (B)** `[gate-1-residual-is-naturalization]` `[naturalization-open-question]`. Gate 1 is not fully discharged: Ilango's caveat (lines 620–625) — a non-recognizable S *might* have a (P/poly)-recognizable Smolensky-style variation S'. If such an S' exists for the Lupanov LB, Gate 1 re-tightens (a recognizable variation usable in the reduction ⇒ Ilango collapse). So the residual Gate-1 risk is *exactly* the naturalization question (B): does the Lupanov/direct-sum tight-window LB admit a recognizable variation, or is it genuinely non-naturalizing?

**What this changes in the map.** (1) The second-loop "two partly-independent gates" framing is refined: the gates are **coupled**, not independent — the method that crosses Gate 2 is itself the Gate-1 candidate. (2) Gate 1 is now discharged as soft from **two** independent directions: Cycle 6 (Williams' (⇒) non-targetability — the reduction's *specific* g) + Cycle 7 (Ilango's own theorem — any successful reduction's LB collection is automatically non-recognizable — the *general* filter). (3) The binding barrier is now **precisely** (A) derandomize Lupanov + (B) naturalization — more optimistic in *structure* (one derandomization + one naturalization proof), equally honest in *difficulty* ((A) is the circuit-LB frontier; (B) is open). (4) The near-term tractable seam is unchanged (a structurally-different uniform-AC⁰ reduction / Tell's promise derandomization).

**Did the wall move?** `[wall-stays-loosened]` — No breakthrough (no P≠NP proof, no new circuit LB, no derandomization, no explicit witness). One **structural overcount** is removed: the wall is no longer two independent gates but two coupled tasks (A)+(B), and Gate 1 is resolved soft from two directions. The wall's *locus* sharpens (binding barrier now precisely (A)+(B)), but the wall does not fall — (A) is the circuit-LB frontier and (B) is genuinely open. `[honest-ceiling]` — primary-source-grounded (Ilango §1.4 lines 559–629 read verbatim); the collapse is a **structural interpretation** (the coupling of the two gates via the Lupanov method), not a new theorem; non-recognizability of the Lupanov LB is **plausible-not-proven** (Ilango's own "does not obviously naturalize"); (A) = circuit-LB frontier; (B) = open. See sources/2026-08-23-two-gates-overcounted.md.

## Third loop — Cycle 8 (2026-08-23): the naturalization question (B) reduces to (A) — recognizable variations are one-sided; the wall's binding barrier simplifies to (A) `[third-loop-cycle-3]` `[b-reduces-to-a]` `[wall-collapses-to-a]`
Cycle 7 collapsed the two-gate wall to (A) derandomize Lupanov + (B) the naturalization question, and named (B) as the residual Gate-1 risk: does the Lupanov/direct-sum tight-window LB admit a (P/poly)-recognizable variation S′ (Ilango's Smolensky-style caveat), and if so does it re-tighten Gate 1? Cycle 8 (sources/2026-08-23-naturalization-reduces-to-a.md) attacks (B) directly — re-reading Ilango §1.4 (`_tr20-183.txt` content-lines 620–629, read VERBATIM) — and finds (B) is (likely) NOT an independent obstacle: it splits into existence vs. usability, and its re-tightening branch reduces to (A).

- **Finding 1 — Ilango's OWN caveat answers the re-tightening sub-question: a recognizable modification S′ (likely) loses the ability to prove (C)-MCSP hardness** `[ilango-recognizable-modification-loses-hardness]`. Verbatim (lines 626–629): "if a collection of lower bound statements S is used to prove hardness for (C)-MCSP, then any (P/poly)-recognizable modification S′ (likely) loses the ability to prove hardness of (C)-MCSP, so it seems like some 'interesting' lower bounds must be lost in this case." So (B) splits: (B-existence) does a recognizable S′ exist (open; Smolensky-analogy plausibly yes) vs. (B-usability) is it usable to prove (C)-MCSP hardness (Ilango: (likely) NO). The re-tightening scenario needs BOTH = yes; Ilango directly says (B-usability) is (likely) no.
- **Finding 2 — the mechanism, grounded in the landscape: recognizable natural properties that EXIST are ONE-SIDED, and fail the tight-window UPPER bound** `[recognizable-variations-one-sided]` `[tight-window-upper-bound-linchpin]` `[cwy-2023-blackbox]`. Chen-Williams-Yang ITCS 2023 (Thm 1.7/1.8/1.11) show recognizable natural properties against AC⁰/formulas DO exist (so (B-existence) leans YES) — but they are one-sided (lower-bound only, "useful against size-s"), failing the tight-window upper bound (g easy at (1+4δ)T, the Lupanov constructive side, Claims 30–31) — the SAME feature Sipser/RST fail (Gate 2, Cycle 1). A one-sided recognizable S′ accepts all-hard functions, does NOT pin the narrow band [(1−4δ)T,(1+4δ)T], hence cannot substitute in the reduction (the reduction needs the tight window) — confirming Ilango's "(likely) loses the ability" with a concrete mechanism.
- **Finding 3 — (B)'s re-tightening branch requires recognizing the tight window, which is ≥ (A)** `[b-reduces-to-a]` `[wall-collapses-to-a]`. The ONLY recognizable S′ that could re-tighten must pin the tight window — recognizing the lower bound (antiprox = (A)) AND the upper bound (MCSP-type) from the truth table; recognizing a fine-grained LB over all truth tables is ≥ constructing one (= (A)), and the upper-bound check is a (C)-MCSP-type decision. So (B-re-tightening) ⟹ (A); the residual Gate-1 risk is NOT an independent obstacle below the (A) frontier.
- **The unification — the tight-window UPPER bound is the single linchpin coupling Gate 1 and Gate 2** `[tight-window-upper-bound-linchpin]`. One feature — g easy at (1+4δ)T, Lupanov's constructive upper bound (Claims 30–31) — is what Sipser/RST fail (blocks them from being tight-window witnesses, Gate 2) AND what recognizable variations fail (one-sided, blocks them from substituting in the reduction, Gate 1 residual). Recognizing/pinning it is the hard part = antiprox + MCSP-type = (A). The two gates, framed as "partly independent" (Cycle 1) then "coupled via the Lupanov method" (Cycle 7), are now coupled via this ONE feature.

**What this changes.** (1) The wall's binding barrier simplifies from (A)+(B) (Cycle 7) to **essentially (A)**: (B) is (likely) not independently binding (Ilango's caveat + one-sidedness), and where it would bind it is ≥ (A). (2) Gate 1's residual risk (the naturalization question) is discharged (likely) from a THIRD independent direction: Cycle 6 (Williams' (⇒) non-targetability — the specific g), Cycle 7 (Ilango's automaticity — the general filter), Cycle 8 (recognizable variations are one-sided — the variation landscape). (3) Gate 1 and Gate 2 are coupled via ONE feature (the tight-window upper bound), not two gates. (4) The near-term tractable seam and the long-term breakthrough shape (an explicit tight-window DNF LB = (A)) are unchanged.

**Did the wall move?** `[wall-stays-loosened]` — No breakthrough (no P≠NP proof, no new circuit LB, no derandomization, no explicit witness). The wall is unchanged at (A) = the circuit-LB frontier. But the map reaches maximum structural compression: the binding barrier is ONE task (A), the two gates are coupled via ONE linchpin feature (the tight-window upper bound), and Gate 1 is discharged soft from THREE directions. More optimistic in STRUCTURE (one task, not two; one linchpin, not two gates), equally honest in DIFFICULTY ((A) is the circuit-LB frontier; Ilango's "(likely)" hedge is the residual; (B-existence) remains open but (likely) irrelevant to re-tightening). `[honest-ceiling]` — primary-source-grounded (Ilango §1.4 lines 620–629 verbatim); the (B)→(A) reduction is a structural interpretation (recognizing ≥ constructing for the lower bound), not a theorem; CWY23 web-confirmed not PDF-line-verified. See sources/2026-08-23-naturalization-reduces-to-a.md.

## Third loop — Cycle 9 (2026-08-23): (A) is intrinsic to the lifting theorem, not a Lupanov-sampler artifact — an alternative explicit function must satisfy the same two (tensioned) conditions `[third-loop-cycle-4]` `[a-is-intrinsic-not-lupanov-artifact]` `[alternative-function-no-bypass]`
Cycle 8 compressed the wall to (A) and proposed attacking (A) from a new angle: a *direct combinatorial* route to an explicit tight-window DNF LB bypassing derandomization of Lupanov — e.g., substituting an alternative explicit function with a known *tight* DNF LB (parity at 2ⁿ, the addressing function at ~2^k). Cycle 9 (sources/2026-08-23-a-is-intrinsic-lifting-conditions.md, **primary-source-grounded** in Ilango FOCS 2020 §1.3, `_tr20-183.txt` content-lines 340–388 read VERBATIM — the Theorem 4/5 informal exposition + the "how do we get our hands on such g" passage) tests this and finds the alternative-function angle does **not** bypass (A): it recovers the SAME two conditions, because those two conditions are the lifting theorem's intrinsic requirements on g.

- **Finding 1 — the lifting theorem (Theorem 5) requires g to satisfy two conditions, which are exactly (A)'s two sides** `[lifting-needs-expensive-and-small-gap]`. Verbatim (lines 375–383): g must (1) be **expensive** relative to f (hard to weakly one-sided-approximate via nondeterministic formulas = the antiprox/lower-bound side = (A)-lower) AND (2) have **L^{AND_{d−1}}(g) − L^{OR}_d(g) small** (the tight-window/small-gap condition = (A)-upper, the Cycle-8 linchpin). Uniformly random functions are expensive (✓) but have a large gap (✗ for d=3); Lupanov's construction gives functions "essentially optimally computed by CNFs" (small gap, ✓) "but also have properties expected of random functions" (expensive, ✓).
- **Finding 2 — the two conditions are in TENSION; Lupanov threads the needle** `[expensive-tensions-small-gap]`. Verbatim (lines 385–387): "as g gets more expensive L^{AND_{d−1}}(g) − L^{OR}_d(g) also gets larger." So harder ⟺ larger gap — the two conditions pull against each other. Lupanov threads the needle (near-optimal CNF ⇒ small gap, yet random-like ⇒ expensive); an explicit construction must thread the same needle.
- **Finding 3 — (A) is intrinsic to the lifting theorem; an alternative function does not bypass it** `[a-is-intrinsic-not-lupanov-artifact]` `[alternative-function-no-bypass]` `[a-is-a-balanced-point]`. Because the two conditions are the lifting's intrinsic requirements on g (not sampler-specific), an alternative function must satisfy BOTH = solve (A). Parity (tight DNF LB at 2ⁿ) is expensive but too hard (large gap, ✗ condition 2); an easy function has small gap but isn't expensive (✗ condition 1); no known explicit function threads the needle — the Lupanov sampler is the only known (randomized) way. (A) is not "an explicit hard function" (parity) nor "an explicit small-gap function" (easy) but a function at the **balanced point** where hardness and small-gap coexist — the Lupanov sweet spot, made explicit.

**What this changes.** (1) (A) is re-described precisely and confirmed intrinsic to the lifting theorem (not a Lupanov-sampler artifact) — the wall is robust to the function choice. (2) The tension sharpens why (A) is hard: not enough to find an explicit hard function (parity, too hard) or an explicit small-gap function (easy, not expensive); one needs the balanced point. (3) The two-gate/linchpin structure (Cycle 8) is confirmed from inside the lifting theorem (condition 1 = antiprox/lower = a gate; condition 2 = small gap/upper = the linchpin). (4) No bypass found; the near-term seam and long-term breakthrough shape are unchanged.

**Did the wall move?** `[wall-stays-loosened]` — No breakthrough. The wall is unchanged at (A) = the circuit-LB frontier. This is a consolidation/robustness cycle: (A) confirmed intrinsic, re-described as the balanced point of two tensioned conditions, the two-gate/linchpin structure confirmed from inside the lifting. Equally honest in difficulty ((A) is the circuit-LB frontier; the tension sharpens why; no impossibility claimed). `[honest-ceiling]` — primary-source-grounded (Ilango §1.3 lines 340–388 verbatim, the tension verbatim); the identification of the two lifting conditions with (A)'s two sides is a structural reading (the exact DNF-vs-AND_{d−1}/OR_d depth accounting kept at structural level, flagged); no impossibility claimed (the tension is difficulty-sharpening, not a no-go); "no known explicit function threads the needle" is survey-level, not exhaustive. See sources/2026-08-23-a-is-intrinsic-lifting-conditions.md.

## Third loop — Cycle 10 (2026-08-23): SYNTHESIS of the third 5-cycle loop — the S1.a wall compressed from a 2-D "two independent gates" barrier to a 1-D "balanced point of two tensioned lifting-intrinsic conditions" barrier `[third-loop-synthesis]` `[third-loop-cycle-5]`
A synthesis cycle (sources/2026-08-23-third-loop-synthesis.md) — grounded in the primary-source passages already read verbatim across the loop (Ilango §1.3 content-lines 340–388 [C9]; §1.4 content-lines 559–629 [C7–C8]) and prior sources, NOT a fresh reading. The loop's net structural product:

- **2-D → 1-D compression** `[two-dim-to-one-dim-wall]`. The wall entered (2nd-loop C4) as a 2-D "cross two PARTLY-INDEPENDENT gates" barrier (Gate 1 = recognizability, Gate 2 = tight window) and exits as a 1-D "hit one balanced point" barrier. The two gates are not independent: Gate 1 is automatic for the Lupanov method (C7) and (likely) re-tightened only via a variation reducing to (A) (C8); Gate 2 and the Gate-1 residual couple through ONE linchpin — the tight-window upper bound (C8); confirmed from inside the lifting as the two intrinsic requirements on g (C9).
- **The balanced point is non-compositional** `[balanced-point-non-compositional]`. The naive decomposition "find an explicit hard function, then make it small-gap" (or reverse) is DOOMED by the tension (expense up ⟹ gap up, verbatim). The construction must be simultaneous/holistic — hardness and small-gap from the SAME structural feature (as Lupanov's near-optimal CNF does). Optimistic in dimension (one object, not two); pessimistic in method (no compositional route).
- **Gate 1 soft from three directions** `[gate-1-soft-three-directions]`. C6 Williams (⇒) non-targetability (the specific g), C7 Ilango automaticity (any successful reduction's LB collection), C8 recognizable-variation one-sidedness (the variation landscape). Gate 1 is a soft filter, not a binding gate; its residual is the "(likely)" hedges.
- **The one new falsifiable target — circular to (A)** `[decouple-expense-from-gap]`. The compression surfaces a precise target: a construction in which expense and small-gap are NOT coupled (Lupanov couples them; a decoupled explicit family = an explicit tight-window DNF LB = (A)). CIRCULAR to (A) — a sharper description of the single lever, not a new lever.
- **The residual hedge on the compression's own obstruction** `[tension-is-lupanov-observed-not-proven-inherent]`. The tension is observed VERBATIM for Lupanov (lines 385–387) but NOT proven inherent to ALL explicit balanced-point constructions — a (likely)-inherent-but-not-proven obstruction, parallel to how (B) was (likely)-non-independent. No impossibility claimed.

**Honest deliverable across the third loop.** The AC⁰ (S1.a) face's wall driven to maximum structural compression and confirmed robust: one intrinsic binding task (A = construct an explicit function at the balanced point of {expensive ∧ small-gap}, = the circuit-LB frontier, an OPEN construction), one linchpin feature (the tight-window upper bound), Gate 1 soft from three directions, no bypass via alternative function (C9) or recognizable variation (C8). No breakthrough. The structural gain (2-D → 1-D, with one linchpin and Gate 1 soft) is real and is the honest product; equally honest in difficulty (the 1-D barrier is non-compositional, so compression does not make it easier to attack, only more precisely described). `[mining-face-forward-pointer]`: the third loop stayed on the S1.a (DNF) face; the symmetric Williams/mining (MAJ∘MAJ) face — compressible to a one-linchpin structure? does its near-term relaxation seam (Tell's quantified derandomization = the CSS16 wire frontier) offer a lever the S1.a face lacks? — is the natural fourth-loop entry. See sources/2026-08-23-third-loop-synthesis.md.

## Fourth loop — Cycle 11 (2026-08-23): the mining (MAJ∘MAJ) face's lever (Tell quantified-derand) confirmed REAL + quantified, guarded by the NEW black-box natural proof barrier; the two faces of [witness-needs-explicit-lb] have DIFFERENT geometry `[fourth-loop-cycle-1]` `[tell-quantified-derand-is-a-real-lever]` `[black-box-natural-proof-barrier]`
Tests the third-loop synthesis's `[mining-face-forward-pointer]` (sources/2026-08-23-mining-face-lever-and-bbnp-barrier.md; web-verified primary literature — Goldreich-Wigderson STOC 2014, Tell STOC 2018, Chen-Tell STOC 2019, Chen-Jin-Williams STOC 2020, Fan-Li-Yang STOC 2022 — search/arXiv-summary-level, NOT PDF-line-verified). Resolves the forward pointer: **YES the mining face has a real lever the S1.a face lacks — and it is guarded by a NEW barrier.**

- **The lever is real and quantified** `[tell-quantified-derand-is-a-real-lever]` `[wire-frontier-quantitative-gap]`. The Goldreich-Wigderson dichotomy `[goldreich-wigderson-dichotomy]`: quantified derandomization (badness B(n)) vs standard is a CLASS-DEPENDENT dichotomy — for STRONG classes (compute extractors + approximate majority, e.g. AC⁰[2]) quantified derand at subexponential B is AS HARD AS standard derand (no LB bypass); for WEAK classes (AC⁰) strictly easier. TC⁰/MAJ∘MAJ is on the STRONG side — Tell (STOC 2018) constructs a seeded extractor in TC⁰ with n^{1+O(1/d)} wires `[tc0-extractor-enables-error-reduction]` (the FIRST TC⁰-specific extractor, the error-reduction bridge); his own theorem: a wire improvement from n^{1+exp(−d)} (his algorithm) to n^{1+O(1/d)} ⟹ standard derand of all of TC⁰ ⟹ NEXP ⊄ TC⁰ (and via CAPP-equivalence, NEXP ⊄ THR∘THR). The lever is QUANTITATIVE — a measurable incremental gap with a known algorithm below threshold and the LB above it, fundamentally unlike S1.a's qualitative all-or-nothing (A).
- **The crossing route is guarded by a NEW barrier** `[black-box-natural-proof-barrier]` `[sharp-threshold-at-phi]`. Fan-Li-Yang STOC 2022 (a fine-grained Razborov-Rudich variant specific to BOOTSTRAPPING): they pin the exact PRF complexity for TC⁰ to n^{1+O(φ^{−d})} wires (φ ≈ 1.618, matching Chen-Tell's threshold), and show that combining a black-box LB technique reaching the threshold with the bootstrapping would DISPROVE PRFs — so NO black-box (natural-proof-style) LB technique can cross; crossing needs a NON-black-box method.
- **The two faces have different geometry** `[mining-face-structurally-different]`. The S1.a compression lens (2-D balanced point with tension) transfers only coarsely — the mining wall is a 2-D gap (strength × regime) blocked by ONE mechanism (boosting/aggregation), not a balanced point of two tensioned conditions; the mining "tension" (more-wires⟹easier) is a banal monotonicity + the non-trivial boosting mechanism, vs S1.a's non-trivial lifting-intrinsic tension.
- **The higher unification** `[two-faces-two-np-variants]`. BOTH faces guarded by a natural-proofs-VARIANT, but on DIFFERENT axes: S1.a = Ilango's recognizability barrier (the witness object's constructivity); mining = Fan-Li-Yang's black-box barrier (the proof method's black-box-ness) — the two RR axes (constructivity vs black-box) as the guarding barriers on the two faces; defeating one does not defeat the other.

**Did the wall move?** `[wall-stays-loosened]` — No breakthrough. The mining payoff (NEXP ⊄ TC⁰ / THR∘THR) remains open, now precisely framed as "cross the wire threshold (n^{1+exp(−d)} → n^{1+φ^{−d}}) with a non-black-box LB method." The honest net: the mining face is MORE optimistic than S1.a (a quantitative lever with an algorithm in hand, vs a qualitative construction) AND guarded by a NEW barrier (black-box natural proofs) the S1.a analysis lacked. Equally honest in difficulty (the crossing needs a non-black-box method, and the barrier says black-box methods can't). `[honest-ceiling]` — web-confirmed not PDF-line-verified; structural comparison is a reading; the load-bearing "real lever" fact is Tell's own stated implication. See sources/2026-08-23-mining-face-lever-and-bbnp-barrier.md.

## Fourth loop — Cycle 12 (2026-08-23): the mining face has TWO stacked barriers on TWO axes (n²-wall + black-box NP); the known methods each escape ONE; Chen-Tal-Wang inhabit the target quadrant but NOT at the mining regime `[fourth-loop-cycle-2]` `[two-barriers-two-axes-mining-face]` `[chen-tal-wang-inhabits-target-quadrant]`
Pursues Cycle 11's directive (does a non-black-box LB method exist for MAJ∘MAJ at the threshold wire count?), via web-verified primary literature (Williams 2011/2014; Fan-Li-Yang STOC 2022; Chen-Tal-Wang STOC 2026 / ECCC TR26-039 — search/arXiv-summary-level, NOT PDF-line-verified; see sources/2026-08-23-mining-face-two-barriers-two-axes.md). Cycle 11 framed the mining face as ONE barrier (black-box NP); Cycle 12 ADDS the second, stacked barrier and reveals the two-barrier/two-axes structure.

- **The Williams method IS non-black-box** `[williams-method-is-non-black-box]`. It exploits the internal ACC structure (SYM+ representation + Evaluation Lemma), circumvents natural proofs via diagonalization (SUCCINCT 3SAT), and is non-relativizing; black-box/oracle methods cannot see this structure. So Williams escapes the Fan-Li-Yang black-box barrier — the mining-axis analogue of how, on the S1.a recognizability axis (Cycles 6-8), Williams' (⇒) is non-targetable. The two axes (recognizability of the witness OBJECT vs black-box-ness of the PROOF METHOD) are independent dimensions of "natural-ness," confirmed by Williams' differing status across them.
- **BUT the mining face has a SECOND barrier the Williams method does NOT escape — the n²-wall — and the two barriers are on TWO ORTHOGONAL axes** `[n2-wall-and-bbnp-are-two-axes]` `[two-barriers-two-axes-mining-face]`. Williams needs a fast SAT algorithm for the class; the n²-wall kills the SYM+ route for TC⁰/THR∘THR at threshold wires (threshold approximate degree Θ√t forces ≥2ⁿ monomials at n² wires). So: **#1 the n²-wall** (ALGORITHMIC axis — does the class admit a fast SAT/CAPP algorithm via structure exploitation) and **#2 the black-box NP barrier** (PROOF-THEORETIC axis — is the LB technique black-box). The known methods each escape ONE and fall to the OTHER: Williams = non-black-box (escapes #2) but n²-wall-blocked (falls to #1); the CSS16/Tell CAPP route = escapes #1 (SAT→CAPP bypasses the fast-SAT requirement) but black-box `[capp-route-is-black-box]` (CSS16's random-restriction LB is, per Fan-Li-Yang, "a black-box natural property extracted from a white-box restriction lemma") → blocked by #2.
- **For the Williams method the two barriers COUPLE through SYM+** `[williams-sym-plus-coupling]`. Its non-black-box-ness IS its SYM+ structure-exploitation, exactly what the n²-wall denies for thresholds; it escapes Fan-Li-Yang precisely where the n²-wall does not apply (ACC⁰), and for TC⁰ both fire through the same mechanism — the mining analogue of how the S1.a two gates coupled through one linchpin (the tight-window upper bound).
- **The target quadrant is INHABITED — by Chen-Tal-Wang — but NOT at the mining regime** `[chen-tal-wang-inhabits-target-quadrant]` `[target-quadrant-not-at-mining-regime]`. Chen-Tal-Wang (STOC 2026, ECCC TR26-039; previously recorded as "STOC 2025" — minor dating correction, additive) apply the Williams method (non-black-box) with a NEW 2^{n−n^{Ω(ε)}} CAPP algorithm for an XOR-of-two n^{2.5−ε}-size THR∘THR circuits (a DIFFERENT route than the single-polynomial SYM+ SAT the n²-wall kills) ⟹ a superquadratic worst-case LB (f ∈ E^NP requires n^{2.5−ε}-size THR∘THR, breaking the decade-old sub-quadratic gate barrier). BUT it does NOT reach the mining payoff: worst-case (not the average-case/correlation LB the NW mining reconstruction needs); n^{2.5−ε} SIZE (not the threshold wire count n^{1+φ^{−d}}); f ∈ E^NP (not NW-usable); CAPP for XOR-of-two specialized circuits (not a general MAJ∘MAJ CAPP).

**Refined genuine-open shape.** A non-black-box LB technique that escapes the n²-wall AND reaches the mining regime (average-case / threshold wire count / NW-usable / general-MAJ∘MAJ). Chen-Tal-Wang show the first two conditions (non-black-box ∧ escapes-n²-wall) are jointly satisfiable — at a weaker regime — so the question reduces to whether their (or a descendant) technique can be pushed from (worst-case, n^{2.5−ε}, E^NP, XOR-of-two) to (average-case, n^{1+φ^{−d}}, NW-usable, general-MAJ∘MAJ). No breakthrough; `[honest-ceiling]` — web-confirmed not PDF-line-verified; the 2×2 classification, the SYM+ coupling, and the quadrant classification are STRUCTURAL READINGS (not theorems); the date corrected additively; no impossibility claimed (the quadrant is inhabited ⇒ the mining payoff is open, not ruled out). See sources/2026-08-23-mining-face-two-barriers-two-axes.md.

## Fourth loop — Cycle 13 (2026-08-23): the within-quadrant push is NOT barrier-free; Cycle 12's "Williams escapes Fan-Li-Yang" PARTIALLY WALKED BACK (two senses of non-black-box; the LB yields a black-box-constructive distinguisher) `[fourth-loop-cycle-3]` `[within-quadrant-not-barrier-free]` `[cycle-12-walk-back]`
Pursues Cycle 12's within-quadrant directive (can the Chen-Tal-Wang XOR-of-two CAPP technique be pushed toward the mining regime, or does pushing there reintroduce a barrier?), via web-verified primary literature (Chen-Oliveira-Santhanam CCC 2016/2017, Chen-Ren STOC 2020, Chen-Williams-Yang ITCS 2023 — search/arXiv-summary-level, NOT PDF-line-verified; see sources/2026-08-23-within-quadrant-push.md). Cycle 12 framed the target quadrant as inhabited (Chen-Tal-Wang) but not at the mining regime; Cycle 13 finds the push from the inhabited corner to the mining regime is NOT barrier-free — on TWO fronts — and PARTIALLY WALKS BACK Cycle 12's clean "Williams escapes Fan-Li-Yang" framing.

- **The Williams method is INHERENTLY WORST-CASE; the average-case regime is a SEPARATE (limited) step** `[williams-lb-inherently-worst-case]` `[avg-case-needs-separate-amplification]`. Chen-Oliveira-Santhanam show the easy-witness step "simply does not work if our assumption is that NEXP is in poly-size ACC⁰ ON AVERAGE rather than in the worst case" — so Chen-Tal-Wang's worst-case n^{2.5−ε} LB does NOT extend to the average-case mining regime by a tweak. Average-case needs a SEPARATE hardness-amplification step (via locally-list-decodable ECCs from the strengthened (NE∩coNE)/1 LB), itself black-box-monotone-limited (black-box monotone hardness amplification cannot amplify even to 0.99 — monotone ECCs lack good distance), OR the Chen-Ren `[capp-implies-strong-avg-case]` CAPP→strong-average-case route (non-trivial CAPP ⟹ strong 1/2+1/n^{ω(1)} average-case LB for nondeterministic time classes, via PCP-of-proximity + crypto-in-NC⁰) — which lands in a nondeterministic-time / structurally-determined (PCP-verifier-witness) regime, NOT an NW-usable / general-MAJ∘MAJ function (the average-case analogue of the witness gap).
- **Cycle 12 conflated TWO SENSES of "non-black-box"; the Williams LB YIELDS a black-box-constructive distinguisher (the axis Fan-Li-Yang is on)** `[two-senses-of-non-black-box]` `[williams-lb-is-black-box-constructive]` `[cycle-12-walk-back]`. Sense 1 — non-black-box PROOF METHOD (exploits internal SYM+ structure, non-relativizing, diagonalizing; Cycle 12 CORRECT here; the LB is non-NATURAL by dropping LARGENESS). Sense 2 — the LB YIELDS a black-box-CONSTRUCTIVE DISTINGUISHER: Chen-Williams-Yang ITCS 2023 `[cwy-2023-blackbox]` show NEXP ⊄ C ⟺ a black-box constructive property (decidable in randomized polylog(N) time WITH ORACLE ACCESS), and algorithmic-method LBs → coRTIME[polylog(N)]-constructive (advice removed). So the Williams/Chen-Tal-Wang LB yields a black-box (oracle-query) constructive distinguisher — precisely the axis Fan-Li-Yang's barrier is on. On the distinguisher axis the Williams LB is BLACK-BOX-constructive, NOT non-black-box; the escape from Fan-Li-Yang is from the LARGENESS axis (non-natural), NOT the black-box-distinguisher axis. Reconciled with the wiki's own `[cwy-2023-blackbox]` (Cycles 3/8, S1.a recognizability axis): CWY2023's black-box-constructivity-unavoidability grounds BOTH RR axes (S1.a recognizability + mining black-box), confirming `[two-faces-two-np-variants]` — the two axes are distinct but commonly rooted.
- **The residual: does Fan-Li-Yang's barrier require LARGENESS?** `[fly-largeness-residual-open]`. The walk-back is PARTIAL because a residual remains. If Fan-Li-Yang's barrier requires LARGENESS (like full Razborov-Rudich), the Williams non-large black-box-constructive LB does NOT trigger it (the non-largeness is a genuine escape — Cycle 12's "escape" stands on the largeness sub-axis). If it does NOT require largeness (like Ilango's `[recognizable-stronger-than-natural]` recognizability barrier, which needs no largeness), the non-large black-box-constructive LB DOES trigger it (a non-large black-box distinguisher at the threshold would disprove PRFs), and Cycle 12's "escape" fully fails. PRF-breaking plausibly needs no largeness (a PRF distinguisher targets the SPECIFIC PRF, not a large fraction), weakly suggesting Fan-Li-Yang needs no largeness — but this is a heuristic, NOT established; the search summaries do not state whether Fan-Li-Yang's barrier requires largeness. This is the single sharpest open residual, line-verifiable (read Fan-Li-Yang's barrier theorem for whether the distinguisher must be large).

**Refined genuine-open shape.** A method that is non-black-box on the PROOF axis AND reaches the AVERAGE-CASE mining regime (not via the limited amplification step, into an NW-usable / general-MAJ∘MAJ function) AND either is non-black-box on the DISTINGUISHER axis OR is a black-box-constructive property that does NOT trigger Fan-Li-Yang (the largeness residual) — STRICTER than Cycle 12's "non-black-box ∧ escapes-n²-wall." The within-quadrant push reintroduces barriers on the average-case axis (separate limited amplification step) AND re-opens the black-box question on the distinguisher axis (Cycle 12 walked back). No breakthrough; `[honest-ceiling]` — web-confirmed not PDF-line-verified; the two-senses distinction and the walk-back are STRUCTURAL READINGS (Cycle 12 was CORRECT on sense 1, incomplete on sense 2 — not "wrong"); the within-quadrant-not-barrier-free synthesis is a reading; the largeness residual is genuinely OPEN, not resolved; no impossibility claimed (quadrant inhabited ⇒ mining payoff open, not ruled out). See sources/2026-08-23-within-quadrant-push.md.

## Fourth loop — Cycle 14 (2026-08-23): `[fly-largeness-residual-open]` RESOLVED — Fan-Li-Yang does NOT require LARGENESS (it requires soundness-on-random = non-largeness); RR and FLY partition the black-box-constructive LB space by largeness; CWY's NEXP-specificity bridges the two faces `[fourth-loop-cycle-4]` `[fly-needs-no-largeness]` `[cwy-bridge-two-faces]` `[mining-face-naturalproofs-blocked]`
Cycle 13 left one residual — does Fan-Li-Yang's black-box NP barrier require LARGENESS? Cycle 14 (web-verified — Fan-Li-Yang STOC 2022 definition, Chen-Williams-Yang ITCS 2023 — search/arXiv-summary-level, NOT PDF-line-verified; see sources/2026-08-23-fly-needs-no-largeness.md) resolves it: **FLY does NOT require largeness — it requires the OPPOSITE.**

- **FLY's definition requires soundness-on-random, not largeness** `[fly-needs-no-largeness]` `[fly-soundness-on-random]`. FLY's "black-box natural property" = (black-box constructivity: PPT oracle A, Pr[A^f=1]≥2/3 for f∈P_yes, **Pr[A^g=1]≤1/3 for random g**) + usefulness. The "accept random ≤1/3" is SOUNDNESS-ON-RANDOM — the distinguisher fires only on the SPECIFIC hard functions (P_yes), not on random. This is the PRF-breaker shape and the NEGATION of RR's largeness (RR: random ∈ property w.h.p.; FLY: random ∉ P_yes w.h.p. — the accepted set is a minority). FLY is the LARGENESS-FREE natural-proofs variant catching non-large (specific-function) distinguishers.
- **Dropping largeness escapes RR but lands IN FLY; RR and FLY partition by largeness** `[nonlargeness-not-an-fly-escape]` `[rr-and-fly-partition-by-largeness]`. Williams escapes RR by dropping largeness (non-large property). But FLY requires soundness-on-random = non-largeness — so Williams' non-large LB SATISFIES FLY's condition; dropping largeness moves the LB from the RR variant (large, RR-blocked) to the FLY variant (non-large, FLY-blocked). The two variants partition the black-box-constructive LB space: LARGE → RR-blocked (RR requires largeness; PRFs rule out large natural properties); NON-LARGE → FLY-blocked (FLY requires sound-on-random; PRFs at the threshold rule out FLY black-box natural properties). A black-box-constructive LB at the threshold is natural-proofs-blocked one way or the other — largeness determines WHICH variant, not WHETHER. Robust to the Cycle-13-flagged unverified detail (the partition covers both sound-on-random and fires-on-random cases).
- **CWY's NEXP-specificity is the bridge; the mining face is natural-proofs-blocked for ALL NEXP-LB techniques** `[cwy-bridge-two-faces]` `[mining-face-naturalproofs-blocked]`. CWY2023 `[cwy-2023-blackbox]`: black-box constructivity is UNAVOIDABLE for NEXP LBs (NEXP ⊄ C ⟺ black-box constructive property). Combining: every NEXP LB is black-box-constructive (CWY); every black-box-constructive LB at the threshold is natural-proofs-blocked (RR or FLY, by largeness). So the mining face's target (NEXP ⊄ TC⁰ at n^{1+O(φ^{−d})} wires) is natural-proofs-blocked for ALL NEXP-LB techniques (Williams, Chen-Tal-Wang, descendants). The ONLY escape is a non-NEXP explicit-function LB = the S1.a face, outside CWY's scope (the Easy Witness forcing is NEXP-specific, Cycle 3) and governed by the DIFFERENT Ilango recognizability barrier (Gate 1, the constructivity axis). CWY's NEXP-specificity is the MECHANISM for `[two-faces-two-np-variants]` (Cycle 11): the NEXP-vs-explicit-function distinction determines which natural-proofs variant bites.

**Did the wall move?** No breakthrough (no P≠NP proof, no new circuit LB, no derandomization). This is the strongest walk-back yet: Cycle 12's "Williams escapes Fan-Li-Yang" is FULLY walked back — sense-1 (proof method) in Cycle 13, the largeness-axis in Cycle 14 (FLY needs no largeness, so there is no largeness-axis escape; dropping largeness escapes RR INTO FLY). The mining face is MORE blocked than any prior cycle suggested — the non-largeness "escape" was illusory, and the two faces are now unified via CWY's NEXP-specificity (the mechanism). `[honest-ceiling]` — web-confirmed not PDF-line-verified; the partition follows from the two definitions (robust at the definition level); the CWY-bridge unification is a STRUCTURAL READING (the load-bearing claim — CWY's NEXP-specificity routing NEXP LBs to the black-box/largeness axis — is sound but the precise scope of "which LBs are unavoidably black-box-constructive" is not fully pinned by the search summaries); NO impossibility claimed for NEXP ⊄ TC⁰'s truth (only for its provability by black-box-constructive NEXP-LB techniques); the wall is deepened, not fallen. See sources/2026-08-23-fly-needs-no-largeness.md.

## Fourth loop — SYNTHESIS (Cycle 15, 2026-08-23): the two routes CONVERGE — the mining (NEXP-LB) face CLOSES (natural-proofs-blocked for all NEXP-LB techniques) and redirects to the S1.a explicit-function route; the natural-proofs skin of [witness-needs-explicit-lb] is fully mapped; the S1.a (A) is the single live thread `[fourth-loop-synthesis]` `[two-routes-converge]` `[s1a-is-the-live-thread]`
The fourth loop (Cycles 11–14) tested the third-loop's `[mining-face-forward-pointer]` (is the mining face's wall compressible to a one-linchpin structure? does Tell's quantified-derand seam offer a lever the S1.a face lacks?). The synthesis (sources/2026-08-23-fourth-loop-synthesis.md): the mining face does NOT compress to an open construction the way the S1.a face did — it CLOSES, and redirects to S1.a.

- **The mining face CLOSES; the NEXP-LB route is natural-proofs-blocked for ALL NEXP-LB techniques** `[nexp-lb-route-naturalproofs-closed]`. Every NEXP LB is unavoidably black-box-constructive (CWY2023 `[cwy-2023-blackbox]`); every black-box-constructive LB at the threshold is natural-proofs-blocked (RR if large, FLY if non-large — the Cycle-14 partition by largeness). So NEXP ⊄ TC⁰ at n^{1+O(φ^{−d})} wires is not provable by any black-box-constructive (= all NEXP-LB per CWY) technique. The ONLY escape from (RR ∪ FLY) is a non-black-box-constructive distinguisher, impossible for NEXP LBs per CWY.
- **The only escape redirects to the S1.a explicit-function route; both routes CONVERGE** `[mining-redirects-to-s1a]` `[two-routes-converge]`. The escape is NOT a NEXP LB but a non-NEXP explicit-function LB = the S1.a face, outside CWY's scope (the Easy Witness forcing is NEXP-specific, Cycle 3) and governed by the DIFFERENT Ilango recognizability barrier — which the S1.a third loop already compressed to the open construction (A). So both routes point to the SAME open construction (A = an explicit function at the balanced point of {expensive ∧ small-gap} = the circuit-LB frontier). The first loop: "both routes need a witness"; the fourth loop: "the mining route's natural-proofs skin is fully closed for the known technique class, and its only exit IS the S1.a route's open construction." CWY's NEXP-specificity is the structural reason for the convergence.
- **The natural-proofs skin of [witness-needs-explicit-lb] is now fully mapped** `[np-skin-two-variants-two-axes]`. Two variants on two axes: black-box/largeness axis (NEXP LBs: RR + FLY, partition by largeness) and constructivity/recognizability axis (explicit-function LBs: Ilango `[recognizable-stronger-than-natural]`, strictly stronger than RR). Which variant bites is determined by the NEXP-vs-explicit-function distinction (CWY's NEXP-specificity, the routing mechanism) — giving `[two-faces-two-np-variants]` (Cycle 11) a mechanism.
- **The S1.a (A) is the single live thread after four loops** `[s1a-is-the-live-thread]`. (A) = an explicit function at the balanced point of {expensive ∧ small-gap}, = the circuit-LB frontier, an OPEN construction (not a proven impossibility), 1-D and non-compositional (`[balanced-point-non-compositional]`), Gate 1 soft from three directions (`[gate-1-soft-three-directions]`) — now the convergence point of both routes. The honest asymmetry: the worst-case SAT route is blocked by a PROVEN theorem (`[n2-wall]`); the average-case CAPP/mining route is natural-proofs-blocked for all NEXP-LB techniques and redirects to (A); the explicit-function (S1.a) route is blocked by the OPEN construction (A) — the convergence point, genuinely alive at one point. The mining face, despite its Cycle-11 quantitative-lever optimism, ends FURTHER from a breakthrough than the S1.a face.

**Did the wall move?** No breakthrough (no P≠NP proof, no new circuit LB, no derandomization) across four loops (20 absolute cycles). The wall is DEEPENED, UNIFIED, and REDIRECTED to a single live thread, not fallen. The deliverable is the most unified and precisely-mapped version of the wall (two variants on two axes, routed by CWY's NEXP-specificity; both routes converging on the S1.a (A)) + the single live thread (A). `[honest-ceiling]` — synthesis (structural reinterpretation, no fresh primary-source reading); the convergence and NEXP-LB closure are STRUCTURAL READINGS (the closure is about PROVABILITY by the known technique class, NOT truth — NEXP ⊄ TC⁰ is widely believed true; a non-LB / non-black-box-constructive method is not ruled out); (A) is an open construction, not a proven impossibility. See sources/2026-08-23-fourth-loop-synthesis.md.

## Fifth loop — Cycle 16 (2026-08-23): CROSS-CONNECTION SURVEY — the convergence to (A)=derandomization is robust on two equivalence axes; (A) is an APEPP-style explicit construction; the survey found no connection humans missed, only ones the wiki had under-synthesized `[fifth-loop-survey]` `[a-is-apepp-style-construction]`
The fifth loop (the user's "connections humans have missed" directive) opens with a survey (sources/2026-08-23-cross-connection-survey.md) hunting for cross-connections between the two faces/axes/cycles treated separately across the prior four loops. Grounded via 4 web searches + the Ilango primary-source re-read + grep.

- **The Cycle-15 convergence to (A)=derandomization is robust on TWO equivalence axes** `[derandomization-equiv-needs-largeness]` `[nonlarge-derandomization-equiv-exists]`. The wiki's used mechanism was constructivity-UNAVOIDABILITY (Williams 2013 NEXP ⊄ C ⟺ P-constructive useful property, largeness dropped; CWY 2023 `[cwy-2023-blackbox]` black-box). The survey finds the SECOND axis: the tempting Williams 2016 Thm 1.7 (no P-natural ⟺ derandomization) is the WRONG equivalence — it needs LARGENESS and the wiki's barriers are non-large — BUT Dhayal-Impagliazzo "Unique Properties, Lower bounds & Derandomization" (`[dhayal-impagliazzo-2020-unique]`, cited Cycle 3 but under-used) establishes lower-bound⟺derandomization equivalences for UNIQUE (extreme non-large) properties. So the non-large barriers (both faces) are derandomization-equivalent via Dhayal-Impagliazzo; the convergence to (A)=derandomization holds on two independent axes, not one.
- **(A) is an APEPP-style explicit construction** `[a-is-apepp-style-construction]` `[korten-apepp-hard-truth-table]`. (A) = derandomizing the Lupanov sampler = an explicit construction from a probabilistic-existence argument = an APEPP problem (Korten FOCS 2021 "The Hardest Explicit Construction": hard-truth-table is APEPP-complete under P^NP; APEPP contains PRGs/extractors/Ramsey/rigid-matrices — the universal explicit-construction class). This lens is NEW for the wiki (APEPP/Korten grep-confirmed absent); it places (A) in the precise complexity class of the hardest explicit constructions. Open: is (A) APEPP-complete (= hardest) or merely in APEPP (could be easier)?
- **Two honest negatives** `[n2-wall-lupanov-tension-parallel]`. The n²-wall (mining, approximate degree) and the Lupanov tension (S1.a, DNF size gap) are PARALLEL obstructions (polynomial degree vs circuit size, different math), NOT a shared algebraic root — so no method-breaks-both. And Williams Thm 1.7 (large-variant) does not cover the non-large barriers.
- **A live 2025 development** `[ilango-2025-derandomization-link]`. Ilango ECCC TR25-190 (2025): Rudich's conjecture (the S1.a recognizability barrier) ⟹ Oracle Derandomization Hypothesis false + a single non-uniform proof-complexity generator + non-uniform ZK without interaction — connecting the S1.a barrier to derandomization and proof complexity; the wiki pre-dates it.

**Did the wall move?** No breakthrough. The honest meta-finding `[honest-ceiling]`: the survey did NOT find a connection humans have MISSED — the deepest connections (Williams 2016, Dhayal-Impagliazzo 2020, Korten 2021, Ilango 2025) are all KNOWN to humans; what the wiki had not done is SYNTHESIZE them (it used constructivity-unavoidability but not the non-large derandomization equivalence; it knew Dhayal-Impagliazzo only as a data point; it never engaged APEPP; it pre-dates Ilango 2025). The wall is NOT moved but is now connected to two under-used frameworks (non-large derandomization equivalence, APEPP). Sharpest falsifiable targets for Cycle 17: (Q-apepp-completeness) is (A) APEPP-complete? (Q-unique-match) does Dhayal-Impagliazzo's unique/non-large equivalence match (A)'s balanced-point set B (non-large, not necessarily single-function)? Web-confirmed findings search/arXiv-summary-level, NOT PDF-line-verified.

## Fifth loop — Cycle 17 (2026-08-23): ATTACKING the Cycle-16 open questions — (A) plausibly IN APEPP but NOT complete; the non-large derandomization equivalence is CLASS-LB-only, so the "two-axis robustness" is FACE-ASYMMETRIC (two axes for mining, one for S1.a); an APEPP × MCSP forward pointer `[fifth-loop-cycle-2]` `[a-plausibly-in-apepp-not-complete]` `[two-axis-robustness-face-asymmetric]`
See sources/2026-08-23-apepp-and-unique-match.md. Cycle 16's two open questions attacked via two targeted web searches (Korten APEPP-completeness; Dhayal-Impagliazzo unique-properties scope). `[a-plausibly-in-apepp-not-complete]`: APEPP's complete problem is EMPTY (find a string outside a circuit's range); hard-truth-table is APEPP-complete under P^NP (Korten FOCS 2021). (A) is a specific restricted balanced-point construction, plausibly IN APEPP (explicit construction from the Lupanov probabilistic-existence argument) but NOT complete (a special case, not as hard as the universal hard-truth-table) — mildly optimistic (could be strictly easier than the hardest explicit constructions) but NOT a lever: "in APEPP" is plausible-not-verified (requires the Lupanov argument to take encoding form); "not complete" is a plausibility argument; the natural-proofs/recognizability barrier applies regardless. The wiki's "open construction" framing is consistent with "in APEPP, not complete." `[unique-equiv-is-class-lb-only]` `[two-axis-robustness-face-asymmetric]`: Dhayal-Impagliazzo's unique property = exactly one function per length; the equivalences (ZUE ⊄ C ⟺ P-unique; UEXP ∩ co-UEXP ⊄ C ⟺ P-unique) are CLASS-LB equivalences. (A)'s B is non-large-but-MULTIPLE (not unique), and (A) is explicit-function (not a class LB), so the unique/non-large equivalence does NOT match (A). This WALKS BACK the Cycle-16 "two-axis robustness" to FACE-ASYMMETRIC: the mining (class-LB) face is two-axis-robust; the S1.a (explicit-function) face is ONE-axis (constructivity-unavoidability, indirect) — the non-large derand-equiv is class-LB-specific (parallel to Cycle 3's `[constructivity-forcing-nexp-specific]`). No lever opens (S1.a still blocked, by one axis), but the convergence is sharpened: the S1.a face, the single live thread (`[s1a-is-the-live-thread]`), is additionally the less-cleanly-characterized one. `[apepp-mcsp-connection]`: Korten's hardness-extraction uses an sMCSP oracle; APEPP-complete hard-truth-table = the search version of MCSP-hardness — the (A) frontier meets the MCSP/Route-A frontier at the APEPP × MCSP intersection (the natural Cycle-18 direction). No breakthrough; `[honest-ceiling]` — web-confirmed search/arXiv-summary-level NOT PDF-line-verified; a placement + a walk-back + a pointer, not a breakthrough; the wall is not moved.

## Fifth loop — Cycle 18 (2026-08-23): the APEPP × MCSP × natural-proofs TRIANGLE — three facets (search/decision/recognizability) of one "exhibit a hard truth table explicitly" task (KNOWN); the locality barrier is the bridge (already in the wiki); (A) is the balanced point of TWO condition-pairs at two levels; derandomizing (A) needs an LB `[fifth-loop-cycle-3]` `[apepp-mcsp-np-one-triangle]` `[a-is-two-balanced-points]`
See sources/2026-08-23-apepp-mcsp-np-triangle.md. Pursues Cycle 17's `[apepp-mcsp-connection]` forward pointer. `[apepp-mcsp-np-one-triangle]`: APEPP (search — EMPTY ∈ FZPP^NP, APEPP ⊆ Σ^P_2, hard-truth-table APEPP-complete under P^NP), MCSP (decision — "a universal statistical test for randomness"; "requiring large circuits is a universal pseudorandom property — a test for large circuits bootstraps to break any PRG"; SMCSP search variant replaces the NP oracle), and natural-proofs/recognizability (property — a recognizable large-circuit property IS a PRG-breaker, so the witness must be NON-recognizable) are the SEARCH/DECISION/PROPERTY facets of ONE task — KNOWN to the literature (Korten 2021 makes the triangle explicit; the encoding principle "high-complexity strings MUST have the pseudorandom properties" = APEPP's pigeonhole form); the wiki's gap was SYNTHESIS (three separate threads), not a missed connection. `[locality-barrier-is-the-bridge]`: CHOPRS ITCS 2020 (slightly-superlinear MCSP LBs ⟹ no natural proofs; the locality barrier) is the bridge between the MCSP/APEPP facet and the recognizability facet — but the wiki ALREADY has `[locality-barrier]` (S3 angle); the APEPP lens CONNECTS to (not replaces) it. `[a-is-two-balanced-points]` (net-additive refinement): (A) is the balanced point of TWO condition-pairs at two levels — structural {expensive ∧ small-gap} (Diagnosis 19, Ilango verbatim "as g gets more expensive the gap also gets larger") AND meta {deterministically-CONSTRUCTIBLE (APEPP/derandomization — Lupanov sampler is BPP, derandomizing to deterministic P IS the open task) ∧ NON-RECOGNIZABLE (natural-proofs)}; the meta tension (constructible ⇒ recognizable; non-recognizable ⇒ hard to construct) is the SAME tension as the structural (expensive ⇒ large-gap) — one tension seen two ways; `[balanced-point-non-compositional]` applies at BOTH levels. `[apepp-derand-needs-lb]`: (A) ∈ APEPP ⟹ reduces to EMPTY ∈ FZPP^NP (still randomized, weaker than the BPP Lupanov sampler the wiki already has); DETERMINISTIC FP^NP for EMPTY ⟺ E^NP 2^{Ω(n)} LB (Korten Thm 11), so derandomizing (A) NEEDS an E^NP LB — the APEPP lens CONFIRMS (A) is a genuine derandomization problem, not a lever; derandomization (a) and non-recognizability (b) are TWO requirements (A) must satisfy both. No breakthrough; `[honest-ceiling]` — no missed connection (triangle KNOWN); web-confirmed search/arXiv-summary-level NOT PDF-line-verified; the wall is not moved but (A) sharpened to a two-level balanced point and the wiki's three meta-complexity threads unified as one triangle.

## Fifth loop — Cycle 19 (2026-08-23): DETERMINISM IS RECOGNIZABILITY — the meta-level non-compositionality is a theorem (RR constructivity, via recompute-and-compare); derandomization mechanistically DESTROYS non-recognizability; the two-level balanced point is asymmetric (meta-tight / structural-heuristic) `[fifth-loop-cycle-4]` `[constructible-implies-recognizable]` `[derandomization-destroys-nonrecognizability]` `[recognizer-strength-no-escape]` `[noncompositionality-mechanistic-asymmetric]`
See sources/2026-08-23-determinism-is-recognizability.md. The within-connection push on the Cycle-18 two-level balanced point `[a-is-two-balanced-points]`. `[constructible-implies-recognizable]`: a deterministic construction A of g is mechanically a (P/poly)-recognizer of g's truth table via recompute-and-compare (run A on all n-bit inputs = 2^n·poly(n) = poly(N), N = truth-table length; compare) — the textbook content of Razborov-Rudich CONSTRUCTIVITY; the wiki's `[gate-1-automatic]` (Cycle 7) had the consequence, this identifies the mechanism. A successful deterministic (A)-construction ⟹ "is the output of A" = recognizable + useful + non-large = a FLY property ⟹ natural-proofs-blocked (assuming PRFs) — the barrier mechanized, NOT a new impossibility (conditional on PRFs; about the deterministic-constructible route, not P≠NP's truth). `[derandomization-destroys-nonrecognizability]` (sharpest product): the Lupanov sampler is non-recognizable PRECISELY because randomized (cannot recompute a random sample without the seed); derandomizing (deterministic FP^NP, `[apepp-derand-needs-lb]`) makes it recompute-recognizable = FLY-blocked — the randomness is the SOURCE of the non-recognizability, not a bug to derandomize away; derandomization and non-recognizability are MECHANISTICALLY INCOMPATIBLE (derandomize ⟺ make-recognizable), connecting `[apepp-derand-needs-lb]` + `[gate-1-automatic]` + `[a-is-two-balanced-points]` via one mechanism — the sharpest form of `[witness-needs-explicit-lb]`: the explicit (deterministic) and non-recognizable (NP-escaping) requirements are incompatible because determinism IS recognizability. `[recognizer-strength-no-escape]`: the FP^NP / P^NP/poly escape (stronger recognizer) is closed — a stronger recognizer is a stronger PRG-breaker, so the barrier is at least as strong at P^NP/poly (my inference, NOT web-verified; flagged). `[noncompositionality-mechanistic-asymmetric]`: the two-level balanced point is ASYMMETRIC — META level (constructible ⟹ recognizable) is a THEOREM (RR constructivity via recompute), STRUCTURAL level (expensive ⟹ large-gap) a HEURISTIC (Lupanov-observed `[tension-is-lupanov-observed-not-proven-inherent]`, not proven inherent); the structural level is the softer side where a clever decoupled explicit family could still land, but any structural success forces a meta-level randomized construction ⟹ the open derandomization gap. No breakthrough; `[honest-ceiling]` — the load-bearing mechanism (recompute-recognizability) is textbook RR constructivity (KNOWN); the wiki's products are the asymmetric two-level refinement + the derandomization-destroys-non-recognizability identification; the wall is NOT moved but deepened to its sharpest mechanistic form; (A) remains an OPEN construction.

## Fifth loop — Cycle 20 (2026-08-23): FIFTH-LOOP SYNTHESIS — no missed connection moved the wall; (A) sharpened to its sharpest mechanistic form and remains an OPEN construction; the frontier's lock is a CONSTRUCTION, not a CONNECTION `[fifth-loop-cycle-5]` `[fifth-loop-synthesis]` `[no-missed-connection-moved-wall]` `[wall-sharpest-mechanistic-form]` `[a-remains-open-construction]`
See sources/2026-08-23-fifth-loop-synthesis.md. Synthesizes Cycles 16-19 + the four prior loops (synthesis cycle, parallel to Cycles 10/15; NO fresh primary-source reading, NO new web search). `[no-missed-connection-moved-wall]`: the user's fifth-loop directive ("look for connections humans have missed") found NO connection humans missed — every connection surfaced (Cycles 16-19) was an under-synthesized KNOWN one (Williams 2016, Dhayal-Impagliazzo 2020, Korten 2021, Ilango 2025, CWY 2023, the APEPP × MCSP × natural-proofs triangle, RR constructivity); the "missed connection" attack vector did NOT move the wall. Honest meta-finding: the P≠NP-adjacent frontier is a mature, well-connected landscape where barriers are KNOWN and open constructions IDENTIFIED; the missing piece is a CONSTRUCTION ((A)), not a CONNECTION — the wall's lock is a construction problem, not a connection problem. `[wall-sharpest-mechanistic-form]`: (A) sharpened across the fifth loop — open construction → in-APEPP-not-complete (C17) → two-level balanced point (C18) → mechanistic non-compositionality (C19: determinism = recognizability via recompute = RR constructivity; derandomization destroys non-recognizability; the explicit-deterministic and non-recognizable requirements are incompatible) — the wall at its sharpest MECHANISTIC form. `[a-remains-open-construction]`: across five loops (25 absolute cycles) the wall neither fell nor was proven impossible; (A) = an explicit function at the balanced point of {expensive ∧ small-gap} (structural) = the circuit-LB frontier, satisfying {deterministically-constructible ∧ non-recognizable} (meta) — remains an OPEN construction, the convergence point of both routes, the single live thread `[s1a-is-the-live-thread]`. The overall arc: one obstruction `[witness-needs-explicit-lb]`, progressively sharpened across 5 loops (both-routes-need-witness → AC⁰-thinnest-triple-conjunction → 2-D→1-D balanced point → mining-closes-redirects-to-S1.a → mechanistic-non-compositionality). NO BREAKTHROUGH across five loops; `[honest-ceiling]` upheld — the deliverable is the most unified, mechanistically-sharpest map + the single live thread + the honest meta-finding; the wall is genuinely alive at one point. **FIVE 5-CYCLE LOOPS COMPLETE (25 absolute cycles).** Do NOT start a sixth loop without the user's direction.

## Sixth loop — Cycle 21 (2026-08-23): attacking the one self-flagged-unverified link — the FP^NP escape from FLY is OPEN; the wall is purely structural for FP^NP `[sixth-loop-cycle-1]` `[fly-constructivity-is-corp-bptime]` `[barrier-ceases-above-conp]` `[recognizer-strength-escape-is-open]` `[cycle19-no-escape-was-overextension]` `[fpnp-escape-not-breakthrough]` `[wall-purely-structural-for-fpnp]` `[meta-obstruction-fp-specific]` `[two-faces-constructivity-axis]` `[fpnp-converges-to-williams-template]`

The fifth-loop synthesis concluded the wall's lock is a CONSTRUCTION not a CONNECTION and the next direction is to attack (A) directly. The single weakest, self-flagged-as-unverified link was Cycle 19's `[recognizer-strength-no-escape]` (my own inference, "NOT web-verified") — that constructing (A) in FP^NP (P^NP/poly recognizer) does not escape FLY. Two web searches settled it against the actual definitions: **WRONG — the FP^NP route is NOT FLY-blocked.**

- **FLY constructivity = BPTIME[polylog(N)]** (randomized oracle poly-time); CWY unavoidable = coRTIME[polylog(N)]/log N; RR = P/poly `[fly-constructivity-is-corp-bptime]`. All barrier classes are below the BPP-adversary PRF-security threshold.
- **The barrier CEASES at coNP+** `[barrier-ceases-above-conp]`: useful properties trivially exist at coNP constructivity (exhaustive circuit search) and are uninformative (Williams). P^NP ⊇ coNP, so P^NP/poly is above the ceiling. The barrier cannot extend to P^NP/poly without PRFs secure against NP oracles (false). NEW axis for the map: constructivity-level (P/BPP/coRP = barrier zone; coNP+ = ceases), complementing the largeness axis `[rr-and-fly-partition-by-largeness]`.
- **Cycle 19 corrected** `[recognizer-strength-escape-is-open]` `[cycle19-no-escape-was-overextension]`: the "determinism = recognizability ⟹ FLY" mechanism is valid only for the FP route (P/poly = FLY's class); over-extended to FP^NP (P^NP/poly, above the class). The FP^NP escape is OPEN.
- **NOT a breakthrough** `[fpnp-escape-not-breakthrough]`: escaping FLY removes a SPUROUS meta obstruction (Cycle 19's over-extension), not the real one. The real obstruction is STRUCTURAL — `[apepp-derand-needs-lb]` (Korten Thm 11): deterministic FP^NP for (A) ⟺ E^NP 2^{Ω(n)} LB. So for FP^NP the wall is PURELY STRUCTURAL `[wall-purely-structural-for-fpnp]`; the meta "mechanistic incompatibility" (Cycle 19's sharpest form) is FP-specific, walked back `[meta-obstruction-fp-specific]`; the two-level balanced point decouples to one level (structural) on the FP^NP route.
- **Two faces sharpened** `[two-faces-constructivity-axis]`: mining/class-LB forced to coRP (in the barrier zone, FLY-blocked `[nexp-lb-route-naturalproofs-closed]`); (A)/explicit-function can sit at P^NP (above the zone, unblocked). Opposite fates = opposite constructivity levels.
- **Convergence** `[fpnp-converges-to-williams-template]`: FP^NP construction of (A) ⟺ E^NP LB ⟺ the Williams faster-SAT/CAPP template on the balanced-point class. The two faces re-merge at the FP^NP/Williams-template level. The E^NP LB faces the relativization barrier (Williams overcame it for NEXP/ACC via non-relativizing SAT algorithms; the balanced-point class would need the same — open).

**Status after Cycle 21:** NO BREAKTHROUGH; `[honest-ceiling]` upheld. A real CORRECTION of the wiki's own prior analysis — the first cycle in several to CHANGE a substantive claim rather than re-describe the wall — but correcting an error that removed a *spurious* obstruction is not a breakthrough; the structural E^NP LB remains open and is the sole real obstruction for the FP^NP route. (A) remains an OPEN construction `[a-remains-open-construction]`, the single live thread. The wall is now MORE PRECISE (purely structural for FP^NP) but unmoved. All web-grounded findings search/arXiv-summary-level, NOT PDF-line-verified. Wiki: 16 pages, 37 sources.

## Sixth loop — Cycle 22 (2026-08-23): REFINING Cycle 21 — the structural obstruction is the FULL balanced point (not just "an E^NP LB"); the Williams-template convergence is ASYMMETRIC `[sixth-loop-cycle-2]` `[s1a-class-is-dnf-easy-sat]` `[williams-template-convergence-asymmetric]` `[structural-obstruction-is-full-balanced-point]` `[cycle21-elpnp-lb-under-specified]`

Cycle 21 ([wall-purely-structural-for-fpnp]) correctly removed the META/FLY obstruction for the FP^NP route but framed the remaining structural obstruction as "an E^NP LB" — which could read as "a circuit LB Williams-style methods sometimes deliver." Re-reading the wiki's own §19 (Diagnosis 19, Ilango FOCS 2020 §1.3 verbatim) to identify WHICH class that LB is against finds the framing UNDER-specified the obstruction. This is a self-correcting REFINEMENT (the wiki's recurring honest pattern — second-loop 13→14→15, fourth-loop 22→23), NOT a breakthrough; it tempers Cycle-21's optimism.

- **The S1.a class is DNF / depth-d FORMULAS** `[s1a-class-is-dnf-easy-sat]`: the balanced point is over DNF/depth-d AND_{d−1}/OR_d formulas (§19 verbatim), a FORMULA class, NOT the threshold (MAJ∘MAJ/THR∘THR) class of the mining face. The two faces are different CLASSES, not just different axes.
- **DNF-SAT is EASY** — DNF and depth-d formula SAT are poly-time (textbook). So the Williams faster-SAT template (the nontrivial lever for the mining class, where SAT is hard and a faster-than-2^n algorithm is the breakthrough ingredient) gives only TRIVIAL/known LBs for the S1.a class (parity ⊄ poly-size-DNF, exponential, long-known) `[williams-template-convergence-asymmetric]`. Faster-SAT for DNF is not a lever; it is already free. The two faces do NOT cleanly converge at the *productive* Williams-template level (Cycle 21's `[fpnp-converges-to-williams-template]` over-stated this) — only at the ABSTRACT "both need an E^NP LB" level. Mining = hard-SAT lever exists; S1.a = easy-SAT, no lever — yet S1.a is the face mining redirects to (`[mining-redirects-to-s1a]`) and the one whose meta obstruction was just removed (Cycle 21).
- **The structural obstruction is the FULL balanced point** `[structural-obstruction-is-full-balanced-point]` `[cycle21-elpnp-lb-under-specified]`: (A) = {expensive ∧ small-gap} (§19 verbatim), in tension (as g gets more expensive the gap gets larger). Parity IS expensive for DNF (2^{n-1} DNF size) — so an E^NP LB / EMPTY-derandomization (`[apepp-derand-needs-lb]`, Korten Thm 11) can output a function as hard as parity — BUT parity has a LARGE gap (§19), so parity is NOT the balanced point. The LB yields the EXPENSIVE/existence side; the small-gap is the separate Lupanov-structure side, non-compositional (`[balanced-point-non-compositional]`). Escaping FLY (Cycle 21) is necessary, not sufficient: it removes the spurious meta obstruction but leaves the real structural one — the non-compositional balanced point (A), the original open construction, unchanged.

**Status after Cycle 22:** NO BREAKTHROUGH; `[honest-ceiling]` upheld. A self-correcting REFINEMENT that PREVENTS over-optimism from Cycle 21, not a breakthrough. The wall for the FP^NP route = {meta/FLY removed (Cycle 21)} + {structural = the full balanced point (A), not reducible to a single Williams-attainable E^NP LB}. Cycle 21 was CORRECT that the FP^NP route escapes FLY (meta obstruction removed) but INCOMPLETE in framing the structural obstruction as "an E^NP LB" rather than the full balanced point — refined, walked back. (A) remains an OPEN construction `[a-remains-open-construction]`, the single live thread `[s1a-is-the-live-thread]`; the wall is MORE PRECISE (structurally = the balanced point, meta removed) but unmoved. Grounding wiki-internal (no fresh web search; load-bearing facts from §19 verbatim and `[apepp-derand-needs-lb]`). Wiki: 16 pages, 38 sources.

## Sixth loop — Cycle 23 (2026-08-23): the PIVOT to a genuinely different attack surface — resource-bounded measure / dimension (Lutz) — re-discovers the natural-proofs barrier from the measure side, but surfaces a new 2022 disjoint-pairs handle `[sixth-loop-cycle-3]` `[measure-hypothesis-implies-pnp]` `[rsc-martingale-naturalproof-equivalence]` `[measure-pivot-rediscovers-natural-proofs]` `[disjoint-pairs-characterization]`

Per the user's option (b) — pivot to a genuinely different attack surface — and the criterion that the surface's *proof shape* (not just topic) sidestep the explicit-construction lock `[witness-needs-explicit-lb]`, the chosen surface is **resource-bounded measure and dimension (Lutz)**: its conclusion μ_p(NP) ≠ 0 ⟹ P ≠ NP is a measure-theoretic *existence/density* statement, not "explicitly construct a hard function." First cycle to step outside the natural-proofs/Williams/meta-complexity framework. New concept page: [[resource-bounded-measure]].

- **Measure hypothesis implies P ≠ NP (and much more), OPEN** `[measure-hypothesis-implies-pnp]`: μ_p(NP) ≠ 0 (NP not p-measure-0, i.e. not efficiently bettable) ⟹ P ≠ NP, strictly stronger (negation ⟹ NP = E₂); plus P-bi-immune NP set, E ≠ NE, BPP ⊆ Δ₂^P, AM ⊆ NP/log — consequences not known from P ≠ NP alone.
- **Relativization barrier (oracles both ways)** `[measure-relativization-barrier]`: Kautz-Miltersen random oracle ⟹ μ_p(NP) ≠ 0 (uninformative — random-oracle hypothesis false); Buhrman-Fenner-Fortnow oracle ⟹ μ_p(NP) = 0. So any proof must be non-relativizing — the measure surface inherits `[three-barriers-required]`.
- **Dimension version, weaker, still ⟹ P ≠ NP, also OPEN** `[dimension-weaker-than-measure]`: dim_p(NP) > 0 (gales = generalized martingales, effectivize Hausdorff dimension); μ≠0 ⟹ dim=1 ⟹ dim>0 ⟹ P ≠ NP; Hitchcock 2002 ⟹ MAX3SAT exp-hard. Weaker ≠ escaped (re-collapses via the same bridge).
- **THE CRUX — measure re-discovers natural proofs** `[rsc-martingale-naturalproof-equivalence]` `[measure-pivot-rediscovers-natural-proofs]` `[positive-measure-means-np-pseudorandom]`: Regan-Sivakumar-Cai 1995 bridge martingales and natural proofs (P/poly measure-0 ⟹ natural proof ⟹ no strong PSRGs; natural property ⟹ martingale, Thm 18; honest martingales strengthen RR, Thm 17) — **martingales / measure / natural proofs are facets of the SAME barrier** (the measure facet joins the learning facet `[cikk-2016]` and the search/decision/property facets `[apepp-mcsp-np-one-triangle]`). Proving μ_p(NP) ≠ 0 ≡ proving NP contains a p-random (pseudorandom/hard) set = a hardness statement whose proof re-collapses to `[witness-needs-explicit-lb]`. The pivot CONFIRMS (does not break) the wall — honestly confirming `[no-missed-connection-moved-wall]` (a different surface re-discovers the wall, as the fifth loop predicted).
- **NEW 2022 handle** `[point-to-set-principle]` `[disjoint-pairs-characterization]`: Lutz-Lutz-Mayordomo (TOCS 2022) — the resource-bounded point-to-set principle (dim(X|EXP)=min_g sup_{A∈X} dim^g_p(A)) decomposes class dimension into per-language relativized dimensions; and **dim(disjNP | disjEXP) = 1 ⟹ dim(NP | EXP) > 0** (Thm 6.1; measure analogue Fortnow-Lutz-Mayordomo 2012) — a new combinatorial sufficient condition (disjoint pairs of NP languages) for NP positive dimension, NEW, barrier-profile unmapped. The pivot's one genuinely new falsifiable target — see [[open-problems]].

**Status after Cycle 23:** NO BREAKTHROUGH; `[honest-ceiling]` upheld. The measure pivot is a genuinely-different-shape route (measure-theoretic existence) that re-converges on the same `[witness-needs-explicit-lb]` obstruction — strengthening the wiki's unifying conclusion that BOTH the construction face (S1.a/(A)) AND the measure face bottleneck on the same explicit-construction / hardness lock. The pivot was not pointless: it added a new surface to the map, a fourth facet (measure) to the natural-proofs barrier, and one new falsifiable target (the 2022 disjoint-pairs characterization). (A) remains the single live thread; the measure face is a second, different-shape route re-converging on the same wall. The wall is unmoved; the map is more complete. All findings web-search-grounded, search/arXiv-summary-level NOT PDF-line-verified. Wiki: 17 pages, 39 sources.

## Sixth loop — Cycle 24 (2026-08-23): mapping the disjoint-pairs target's barrier profile — stronger-not-easier, relativization-blocked, re-collapses to an inseparable-pair witness; disjoint NP pairs are the BRIDGE to proof complexity `[sixth-loop-cycle-4]` `[disjoint-pairs-stronger-not-easier]` `[disjoint-pairs-relativize]` `[disjoint-pairs-re-collapse-to-inseparable-pair]` `[disjoint-pairs-bridge-to-proof-complexity]`

Cycle 23 surfaced one genuinely new, unmapped falsifiable target — the 2022 Lutz-Lutz-Mayordomo disjoint-pairs characterization `dim(disjNP | disjEXP) = 1 ⟹ dim(NP | EXP) > 0 ⟹ P ≠ NP`. Cycle 24 (sources/2026-08-23-disjoint-pairs-profile.md; four web searches — Lutz-Lutz-Mayordomo 2022, Fortnow-Lutz-Mayordomo 2009, Fortnow-Rogers 2002, Glaßer-Selman-Zhang/Razborov — search/arXiv-summary-level, NOT PDF-line-verified) maps that target's **barrier profile**. New concept page: [[disjoint-np-pairs]] (the bridge node).

- **Stronger, not easier** `[disjoint-pairs-stronger-not-easier]`: the implication runs *from* `dim(disjNP|disjEXP)=1` *to* `dim(NP|EXP)>0` (Thm 6.1), so the disjoint-pairs condition is **stronger than** (implies) the direct dimension hypothesis it is meant to deliver — a harder-to-prove sufficient condition, not an easier shortcut. Refines Cycle 23's "falsifiable target": the only candidate leverage is the *different combinatorial shape* (pairs + separability), and the items below show it yields none.
- **Relativization: blocked** `[disjoint-pairs-relativize]`: oracles exist on both sides — Fortnow-Rogers 2002 settle all relativized separability relationships with generic oracles; Glaßer et al. oracle O₂ (complete pairs exist, optimal proof systems do not — Razborov's converse fails relativizably); Fortnow-Lutz-Mayordomo 2009 oracle separations. Inherits `[measure-relativization-barrier]` / `[three-barriers-required]` on the relativization axis.
- **Re-collapse to a differently-shaped witness** `[disjoint-pairs-re-collapse-to-inseparable-pair]` `[disjoint-pairs-inherit-rsc]`: full dimension of disjNP = disjNP pseudorandom/dense = no efficient gale succeeds against it ⟹ (via RSC `[rsc-martingale-naturalproof-equivalence]`) exhibiting a hard object — a **P-inseparable (TIME(2^{n^k})-inseparable) disjoint NP pair** (A,B), a *pair* resisting P-separation, not the single balanced-point function (A). Re-collapses to `[witness-needs-explicit-lb]`; the construction is no less open for the different shape. The one-way-function link (P≠UP ⟹ P-inseparable pairs; necessary for public-key crypto) confirms the cryptographic-hardness / natural-proofs shape. Natural-proofs **inherited** via RSC (density ≡ martingale-resistance ≡ natural property).
- **The genuinely new structural fact — the proof-complexity BRIDGE** `[disjoint-pairs-bridge-to-proof-complexity]`: Razborov 1994 — every proof system f has a canonical disjoint NP pair (SAT*, REF_f), and if f is optimal the canonical pair is ≤_m^pp-complete for DisjNP; Glaßer-Selman-Zhang — *every* disjoint NP pair is equivalent to a canonical pair ⟹ DisjNP and canonical-pair degrees have **identical degree structure**. So the disjoint-pairs question is equivalent (in degree structure) to the propositional-proof-system question — disjoint NP pairs are the **bridge** from the measure surface to the proof-complexity surface. **Narrows the pivot space:** "pivot to proof complexity" (one of the four original options) is the *far end of an already-mapped bridge*, not a fresh surface. The far end is itself barriered `[proof-complexity-face-barriered]`: complete pairs ⟺ DisjNP uniformly enumerable (Thm 6.5, "highly unlikely"); optimal proof systems relativization-blocked (oracles both ways); known LBs only for weak systems (resolution, bounded-depth Frege); Frege/Extended-Frege/optimal OPEN. **One unmapped piece:** whether the proof-complexity face meets a natural-proofs-style barrier (in the proof-complexity sense) is NOT web-verified here.

**Status after Cycle 24:** NO BREAKTHROUGH; `[honest-ceiling]` upheld. The disjoint-pairs target does NOT escape the wall on any of the three barrier axes (relativization blocked, natural proofs inherited, explicit construction re-collapsed to an inseparable-pair witness). It re-confirms `[no-missed-connection-moved-wall]` from a **third sub-surface** (structural face → measure face → disjoint-pairs/proof-complexity bridge). The cycle's one genuinely new product is **structural, not a crack**: the disjoint-pairs formulation is the bridge connecting the measure surface to the proof-complexity surface, which **narrows the pivot space** — of the four original pivot surfaces, proof complexity is now adjacent (not fresh); the genuinely-untouched surfaces are **descriptive complexity** and (already partially mapped) **GCT/algebraic**. (A) remains the single live thread (OPEN construction) from the structural side. Wiki: 18 pages, 40 sources.

## Sixth loop — Cycle 25 (2026-08-23): the descriptive/logic-for-P pivot — FIRST surface to escape the relativization barrier `[sixth-loop-cycle-5]` `[logic-for-p-implies-pneqnp]` `[descriptive-escapes-relativization]`

Per Cycle 24's narrowed pivot space (proof complexity now adjacent via the disjoint-pairs bridge; genuinely-untouched = descriptive complexity + GCT/algebraic), this cycle pivots to **descriptive complexity** — not the (already-dead) ESO/0-1-law route, but its orthogonal residual: the **Gurevich logic-for-P question** (sources/2026-08-23-descriptive-logic-for-p.md; four web searches — Dahan-Dawar 2020 / Gurevich 1988 / Dawar 1995; CPT Blass-Gurevich-Shelah 1999 / Pago 2021-2023; Grohe FP+C / CFI 1992; CHOPRS locality — search/arXiv-summary-level, NOT PDF-line-verified).

- **The lever** `[logic-for-p-implies-pneqnp]`: Gurevich (1988) conjectured **no logic captures P** on all finite (unordered) structures; **no-logic-for-P ⟹ P ≠ NP** (via P=NP ⟹ poly-time canonization ⟹ a logic for P; Dahan-Dawar 2020 clarify the argument needs the PH collapse, not just Fagin). Converse open. The conclusion is a **meta non-existence statement**, not "exhibit a hard function" — sidesteps the `[witness-needs-explicit-lb]` construction lock in its *conclusion* (like the measure pivot).
- **The genuinely new structural fact — relativization escape (FIRST in 24 cycles)** `[descriptive-escapes-relativization]`: Dahan-Dawar 2020 construct an oracle A with a logic for P^A but P^A ≠ NP^A ⟹ the lever is a **non-relativizing** theorem (its relativized analog fails) ⟹ any P≠NP proof through "no logic for P" is **automatically non-relativizing** — the relativization barrier `[three-barriers-required]` is circumvented **for free**. **Every prior surface was relativization-blocked** ((A)/S1.a, mining, measure `[measure-relativization-barrier]`, disjoint-pairs `[disjoint-pairs-relativize]`); the descriptive surface is the sole exception. It escapes **one** of three barriers — natural-proofs/algebrization still apply to the *proof*.
- **The catch — active frontier targets a weaker question** `[cpt-separator-not-pneqnp]` `[logic-for-p-harder-not-easier]`: the active frontier (CFI separating CPT / CPT+WSC from P; Pago 2021/2023) targets a **one-logic** separation, NOT the full Gurevich conjecture ⟹ it reaches only CPT≠P, **not P≠NP**. The P≠NP-relevant lever (full "no logic for P") is a universal negative, plausibly **harder** than (A), not easier. Candidate status: FP+C ruled out (CFI 1992), rank logic ruled out (Lichter 2022), **CPT** open (leading candidate; CFI on unordered graphs open), CPT+WSC open (Lichter-Schweitzer 2022).
- **A second independent bridge to proof complexity** `[descriptive-bridges-to-proof-complexity]`: Pago CSL 2023 — CPT-distinguishability ⟹ **EPC3** (degree-3 extended polynomial calculus) distinguishability for GI; a super-poly EPC3 LB for the explicitly-given CFI/multipede families separates CPT from P. The **second** independent bridge to the proof-complexity face (Cycle 24's disjoint-pairs→canonical-pairs bridge being the first) — two surfaces converge on proof complexity as a **multi-surface convergence hub**; the far end is itself barriered `[proof-complexity-face-barriered]`.
- **The re-collapse — weak-model LBs, instance-free** `[descriptive-re-collapse-weak-model-lb]`: the *proof* re-collapses to lower bounds against **weak models** (symmetric XOR-circuits — Pago MFCS 2023; EPC3) for **explicitly-given** instances (CFI/multipedes are in P, freely constructible) — the **construction lock is DROPPED** (first surface where the "exhibit a hard instance" half is satisfied for free), leaving only a LB against a weak model. Whether natural-proofs / `[locality-barrier]` applies to **symmetric** circuits (a restricted, automorphism-invariant class) is itself **open** — a sliver of light, honestly flagged as unmapped, not a known escape.

**Status after Cycle 25:** NO BREAKTHROUGH; `[honest-ceiling]` upheld. The descriptive/logic-for-P surface is the most genuinely *different* surface of the sixth loop: the **first to escape the relativization barrier** and the **first to drop the construction lock** (instance-free weak-model-LB shape). But it does not solve P≠NP — the P≠NP-relevant lever (full Gurevich conjecture) is a universal negative harder than the active CPT-separator frontier, and its *proof* re-collapses to weak-model LBs that bridge to proof complexity (re-confirming the convergence hub) and to restricted-circuit LBs. Re-confirms `[no-missed-connection-moved-wall]` from a **fourth sub-surface** (structural → measure → disjoint-pairs/proof-complexity → descriptive/symmetric-circuits+EPC3). (A) remains the single live thread (OPEN construction); the descriptive surface is a genuinely-different-shape fifth route. **SIXTH 5-CYCLE LOOP COMPLETE (25 absolute cycles + Cycle 25).** Wiki: 18 pages, 41 sources.

## Seventh loop — Cycle 26 (2026-08-23): GCT — the surface that escapes ALL THREE barriers and ISOLATES (not removes) the construction lock; the wall is a UNIVERSAL construction, not LOCAL barriers `[seventh-loop-cycle-1]` `[gct-escapes-three-barriers]` `[gct-flip-is-construction-lock]` `[barriers-and-construction-are-complementary]`

The sixth loop's synthesis named GCT/algebraic as the last genuinely-untouched surface. Cycle 26 (sources/2026-08-23-gct-flip.md; four web searches — Mulmuley overview arXiv:0908.1932 / GCT6 flip arXiv:0704.0229 / arXiv:1009.0246; VP-vs-VNP transfer (Bürgisser 2000, Valiant 1992, Koiran-Perifel, Bürgisser 2024); flip-status negatives (Ikenmeyer-Panova 2017, Bürgisser-Ikenmeyer-Panova JAMS 2019, Ikenmeyer-Mulmuley-Walter 2017, Fischer-Ikenmeyer 2020); Mulmuley "explicit proofs / complexity barrier" — search/arXiv-summary-level, NOT PDF-line-verified) profiles GCT against `[witness-needs-explicit-lb]` and corrects two overstatements in the wiki's existing `gct.md`.

- **CORRECTION — GCT escapes all THREE local barriers, not two** `[gct-escapes-three-barriers]`. The wiki's `gct.md`/`barriers.md` recorded GCT as escaping natural-proofs + algebrization only. Mulmuley's overview (Thm 8.3) claims GCT also bypasses **relativization** (works on the algebrized form of computation, IP=PSPACE spirit), so it bypasses all three **simultaneously** with strongly explicit obstructions — the **first surface in the 26-cycle map with a clean barrier profile**. Natural-proofs escape via **extreme rigidity** (characterization-by-symmetries of det/perm ⟹ |UP_n| ≤ 2^{poly(n)}, bypassing RR largeness, Prop 7.3); algebrization/degree escape via representation-theoretic multiplicity structure (perm=det same degree). **Honest caveat:** Mulmuley's claim at search/arXiv-summary level, NOT PDF-line-verified; the rigidity↔natural-proofs part accepted in the GCT literature but not an RR-framework theorem.
- **The flip IS the construction lock** `[gct-flip-is-construction-lock]`. GCT's defining strategy converts the LB *nonexistence* problem (perm⊄det) into an *existence/construction* problem: exhibit geometric obstructions (Weyl modules) that are **short** (poly(n) bitlength), **verifiable** (poly(n) time), **constructible** (poly(n) time) — NP-witnesses for *nonmembership in P*. The trivial obstruction (exponential counterexample table) has **"no short witnesses"** (Mulmuley). This is precisely `[witness-needs-explicit-lb]` in algebraic guise — the construction lock is not avoided, it is the *entire point* of the flip.
- **Mulmuley's universal complexity barrier = the wiki's witness barrier** `[mulmuley-complexity-barrier-equals-witness-barrier]`. Mulmuley calls the three (four) barriers **"local and mathematical"** and posits a deeper **universal, complexity-theoretic complexity barrier** (self-referential: "discovery is hard" ⟹ "discovering this proof is hard"), whose formalization IS the short-witness/explicit-construction problem. **The wiki's independent six-loop meta-finding** ("the wall's lock is a CONSTRUCTION not a CONNECTION/BARRIER," reached from four sub-surfaces) **converges with Mulmuley's formalization** — an independent convergence of two separate lines on the same obstruction, not a missed connection (Mulmuley knew it).
- **THE SHARPENING — barriers LOCAL, construction UNIVERSAL, complementary** `[barriers-and-construction-are-complementary]`. GCT is the **existence proof** that escaping all three local barriers does NOT remove the construction lock — it **isolates** it. GCT pays to escape all three and is left *exactly* with the flip = the full construction lock. Triangulated with the descriptive surface (Cycle 25: escapes *one* barrier, drops the construction lock on the *instance* side because CFI/multipedes are explicit+in P, but retains the weak-model-LB construction), the two surfaces pin the wall on two orthogonal axes — **barrier-clean yet construction-locked (GCT) vs instance-free yet LB-locked (descriptive)**; no surface is clean on both. **The real obstruction is the construction lock, and no barrier-escape removes it.** The wall is *not* the three barriers (local symptoms); the wall *is* the universal short-witness construction problem. Strongest form of `[no-missed-connection-moved-wall]` and sharpest `[witness-needs-explicit-lb]`.
- **The construction lock bites TWICE** `[flip-construction-objects-are-nphard]`. The flip requires poly-time-constructible obstructions, but the objects — Kronecker positivity (NP-hard, Ikenmeyer-Mulmuley-Walter 2017) and plethysm positivity/computation (NP-hard / #P-hard / GapP-complete, Fischer-Ikenmeyer 2020 = the wiki's `[fi-2020-asymmetry]`) — are themselves NP-hard/#P-hard. The poly-time constructibility the flip requires collides with the very P≠NP-style hardness it proves — the self-referential core, made concrete. (Plus: Bürgisser-Ikenmeyer-Panova JAMS 2019 disproved **occurrence obstructions** — Mulmuley-Sohoni Conjecture 1.3 is FALSE; only the multiplicity-based approach remains.)
- **CORRECTION — VP≠VNP does not imply P≠NP** `[vnp-separation-not-pneqnp]`. The wiki's `gct.md` claimed GCT "transfers to P≠NP via Valiant." Overstated: VP≠VNP ⟹ **P/poly ≠ NP/poly** (over F₂, Valiant 1992; general fields under GRH, Bürgisser 2000) — a *nonuniform* separation; VP=VNP ⟹ PH collapses (under GRH). VP≈NC² (depth reduction), so VP≠VNP ≈ algebraic NC²≠#P, which does **not** rule out P=NP. GCT's target is **necessary, not sufficient** for boolean P≠NP — same "transfer-gap" shape as descriptive (CPT≠P) and mining (NEXP LB).
- **The positivity core is deep OPEN math** `[gct-flip-needs-deep-open-math]`. Theorem 10.2: an explicit obstruction family exists assuming PH1 (positive #P-formulae for multiplicity functions) + OH. Proving PH1 requires extending **LR PH0** (proved via the Riemann Hypothesis over finite fields, Deligne/Lusztig) to **Kronecker PH0** (nonstandard quantum groups, GCT4/7/8) — the top-right corner of Mulmuley's commutative diagram is **unknown**. "Law of conservation of difficulty."

**Status after Cycle 26:** NO BREAKTHROUGH; `[honest-ceiling]` upheld. GCT is the most barrier-clean surface in the entire map — the first to (claim to) escape all three local barriers — and it delivers the **sharpest, most externally-grounded form of the central meta-finding**: the three barriers are *local symptoms*, the construction lock is the *universal wall*, and they are *complementary* (escaping all three barriers leaves exactly the full construction lock = the flip). Mulmuley's independent "universal complexity barrier / no short witnesses" formalization converges with the wiki's `[witness-needs-explicit-lb]` (two separate lines, same obstruction). The construction lock bites twice in GCT (the flip's objects are NP-hard/#P-hard) and runs to the depth of extending the RH over finite fields; and even success separates VP≠VNP (algebraic), not boolean P≠NP. Re-confirms `[no-missed-connection-moved-wall]` from a **fifth sub-surface** (structural→measure→disjoint-pairs/proof-complexity→descriptive→GCT/algebraic). (A) remains the single live thread (OPEN construction); GCT is the barrier-clean route that confirms the wall is the construction, not the barriers. All findings search/arXiv-summary-level, NOT PDF-line-verified. Wiki: 18 pages, 42 sources.

## Seventh loop — Cycle 27 (2026-08-23): DIRECT ATTACK on (A) — the CAPP-weakened Williams template IS productive for easy-SAT classes, but the LB does NOT reach (A); the construction lock is LOCALIZED to the bridge between two corners `[seventh-loop-cycle-2]` `[capp-template-productive-for-easy-sat]` `[cycle22-sat-vs-capp-conflation]` `[clw-lb-non-explicit-refuter]` `[clw-lb-subexp-not-tight-window]` `[construction-lock-localized-two-corners]` `[cwy-blackbox-unavoidable-from-capp-angle]` `[ip-gadget-lifting-proof-complexity-2025]`

Sources/2026-08-23-capp-template-easy-sat.md. After five loops mapping sub-surfaces (structural → measure → disjoint-pairs/proof-complexity → descriptive → GCT/algebraic), Cycle 27 attacks the single live thread (A) DIRECTLY, not another surface. The exact door between "meta obstruction removed" (Cycle 21) and "structural obstruction solved" is the question: is there a non-faster-SAT (CAPP-weakened) Williams-template route to an E^NP / EXP circuit LB for a class with EASY SAT? Four web searches (search/arXiv-summary-level, NOT PDF-line-verified).

- **PARTIAL WALK-BACK of Cycle 22** `[capp-template-productive-for-easy-sat]` `[cycle22-sat-vs-capp-conflation]`: Cycle 22's "the Williams template is trivial for S1.a" is correct only for the **faster-SAT** version. The CAPP-weakened Williams template — weakened historically SAT → nondeterministic Gap-UNSAT (Murray-Williams 2018, Chen-Ren 2020) → CAPP (Chen-Lyu-Williams FOCS 2020 Thm 1.2: "if CAPP of AND_4∘C at 2^{n−n^ε} then an average-case E^NP LB against C") — IS productive for easy-SAT classes, because **deterministic CAPP for DNF/AC⁰/depth-d is known UNCONDITIONALLY** (Gopalan-Meka-Reingold CCC 2012 quasi-poly n^{O((log log n)²)}; Trevisan ECCC TR02-069 linear-time k-DNF; Williams 2014 ACC⁰⊇AC⁰⊇DNF #SAT in 2^{n−n^ε}). Combining CLW Thm 1.2 with these unconditional CAPPs yields an **unconditional non-trivial average-case E^NP LB against DNF/AC⁰/depth-d** — the S1.a class and its superclasses — via CAPP, not faster-SAT. So a non-faster-SAT Williams-template route to an E^NP LB for an easy-SAT class EXISTS, refuting the strong reading of `[williams-template-convergence-asymmetric]`.

- **BUT this LB does NOT reach (A)** `[clw-lb-non-explicit-refuter]` `[clw-lb-subexp-not-tight-window]`, on three independent axes: (i) NON-EXPLICIT — the CLW LB is built by a **P^NP refuter/diagonalization** (Williams-style easy-witness contrapositive), not by the AC⁰-constructible lifting witness g that (A) requires; the certified hard function is the refuter's diagonalization output, existentially specified, not the explicit constructible g at the balanced point (the `[single-lb-implies-class-lb]` / `[vicinity-does-not-retighten]` gap made concrete). (ii) SUBEXPONENTIAL SIZE — the LB is "requires 2^{n^ε}-size C-circuits," SUBEXPONENTIAL, NOT the tight lifting window [(1−4δ)t·n²,(1+4δ)t·n²] = 2^{Θ(n)} that (A) must pin (`[s1a-antiprox-obstruction]`); the CAPP-template's 2^{n−n^ε} resolution translates to a 2^{n^ε} LB, qualitatively coarser. (iii) OFF the balanced point — an E^NP LB yields the EXPENSIVE/existence side only (`[structural-obstruction-is-full-balanced-point]`); the CLW LB provides NO small-gap (Lupanov-structure) side, the non-compositional half of (A) (`[balanced-point-non-compositional]`).

- **The construction lock is LOCALIZED** `[construction-lock-localized-two-corners]`: known explicit LBs (gate-elimination: Li-Yang STOC'22 3.1n affine dispersers; Carmosino-Dang-Jackman 2026 refuters, linear) and known S1.a-class LBs (CLW CAPP-template, average-case E^NP, subexp) occupy DIFFERENT corners of {explicit, tight-window, small-gap, S1.a-class}: gate-elimination = {✓explicit, ✗tight-window, ✗small-gap, ✗S1.a-class (general circuits)}; CAPP-template = {✗explicit (P^NP refuter), ✗tight-window (subexp), ✗small-gap (expensive-side only), ✓S1.a-class}. NO result occupies the balanced point (A) = {explicit ∧ tight-window ∧ small-gap ∧ S1.a-class}; the lock is the BRIDGE between the two corners — making the CAPP LB explicit AND tight-window AND small-gap simultaneously IS (A).

- **CWY from the CAPP angle** `[cwy-blackbox-unavoidable-from-capp-angle]`: the meta-reason the CAPP LB cannot be made explicit re-confirms `[cwy-2023-blackbox]` (Cycles 3/8/14) from a new angle. CWY ITCS 2023: black-box constructivity is unavoidable for NEXP LBs, and a deterministic polylog(N)-constructive property useful against AC⁰ is impossible unconditionally (Thm 1.6). The CLW LB is a class LB (NEXP/E^NP regime) ⟹ unavoidably black-box-constructive; forcing it into a deterministic explicit construction against AC⁰ would contradict Thm 1.6. So non-explicitness (i) is a manifestation of the universal NEXP-LB black-box-constructivity barrier — the SAME mechanism as `[nexp-lb-route-naturalproofs-closed]` (Cycle 14/15): NEXP/class LBs are routed to the black-box/largeness axis and cannot be made explicit without leaving the NEXP-LB regime. (A) lives in the explicit-function regime precisely to escape this, re-encountering the S1.a recognizability barrier. The CAPP angle independently re-derives the two-faces split `[two-faces-two-np-variants]`.

- **The lifting gadget family → proof complexity (2025)** `[ip-gadget-lifting-proof-complexity-2025]`: corroborating. The S1.a lifting gadget family (Ilango's lifting theorem) appears in Bhattacharya-Chattopadhyay ECCC TR25-106 (2025): super-poly Res(⊕) LBs via Tseitin-on-expanders LIFTED with IP gadgets (small correlation with all parities). The SAME gadget machinery that defines (A)'s balanced-point conditions yields genuine LBs in PROOF COMPLEXITY (Res(⊕)), not circuits — the third independent route to the proof-complexity convergence hub (after disjoint-pairs Cycle 24 and descriptive Cycle 25), re-confirming `[proof-complexity-face-barriered]` and `[descriptive-bridges-to-proof-complexity]`: the gadget family's LBs are real but in the wrong (proof-complexity) world for (A).

**Status after Cycle 27:** NO BREAKTHROUGH; `[honest-ceiling]` upheld. The product is a PARTIAL WALK-BACK of Cycle 22 (the CAPP template IS productive for easy-SAT — refuting the strong reading that the Williams template is globally trivial for S1.a) + a precise LOCALIZATION of the construction lock to the bridge between the explicit-gate-elimination corner and the CAPP-template-S1.a corner — neither is (A), and the gap is exactly the explicit+construction-vs-black-box-constructivity and tight-window-vs-subexp axes `[witness-needs-explicit-lb]` / `[apepp-derand-needs-lb]` name. **Attacking (A) directly — not mapping yet another sub-surface — still hits the construction lock: the STRONGEST confirmation yet that the wall's lock is a CONSTRUCTION not a CONNECTION** (`[no-missed-connection-moved-wall]` now from the direct-attack angle, the sixth sub-surface: structural → measure → disjoint-pairs/proof-complexity → descriptive → GCT/algebraic → direct-attack-on-(A)). (A) remains the single live thread (OPEN construction). All findings search/arXiv-summary-level, NOT PDF-line-verified. Wiki: 18 pages, 43 sources. **SEVENTH LOOP, Cycle 2 (absolute Cycle 27) complete — 3 cycles remain (28-30) per the governing mandate unless the user redirects.**

## Seventh loop — Cycle 28 (2026-08-24): the DEPTH/Karchmer-Wigderson surface barrier-profiled — a genuinely non-size axis; evades natural-proofs (plausibly) but is algebrization-blocked; the direct-sum obstacle is the depth-face of the axis-independent non-compositionality lock

`[depth-axis-barrier-profiled-not-untouched]` — honest correction: the depth/KW surface was NOT untouched (the `communication-kw` page already mapped KW/KRW, `[meir-2023]`, `[iyer-rao-2024]`, `[cavalar-oliveira-2025]`, and the P≠NC¹ transfer gap); the cycle BARRIER-PROFILES it against `[witness-needs-explicit-lb]` — the FIRST non-size axis profiled (every prior sub-surface was a size-LB family), the seventh sub-surface (structural → measure → disjoint-pairs/proof-complexity → descriptive → GCT/algebraic → direct-attack-on-(A) → depth/KW-communication). 4 web searches, all search/arXiv-summary-level NOT PDF-line-verified.

`[krw-may-evade-natural-proofs]` — the KRW approach is a natural-proofs-escape candidate: it targets a SPECIFIC constructed function (a random log n-bit function composed with itself O(log n) times, explicit via its n-bit description) rather than a random function, so it may violate RR largeness — the SAME escape-shape as GCT's rigidity escape (`[gct-escapes-three-barriers]`). Second surface (after GCT) with a plausible RR escape, on the depth axis. (Search-summary reading, NOT an RR-framework theorem; the page's randomized-protocol barrier caps discrepancy/rank/information methods at O(log n) regardless.)

`[chen-hu-ren-2026-algebrization-via-communication]` — BUT the surface is ALGEBRIZATION-BLOCKED: Chen-Hu-Ren (ITCS 2026, LIPIcs.ITCS.2026.37) build NEW algebrization barriers via communication complexity of XOR-Missing-String (PostBPE ⊄ i.o.-SIZE[O(n)] needs non-algebrizing; Aaronson-Wigderson improved to MULTILINEAR extensions; half-exponential the correct algebrizing barrier). The depth surface trades one local barrier (natural-proofs) for another (algebrization), re-confirming `[barriers-and-construction-are-complementary]`; unlike GCT (claimed all three), the depth surface escapes at most one.

`[depth-transfer-gap-p-vs-nc1]` — transfer gap: KRW ⟹ P ≠ NC¹, NOT P ≠ NP (formula LBs do not imply general circuit LBs; P/poly ⊋ NC¹ not even known) — same necessary-not-sufficient shape as descriptive (CPT≠P `[cpt-separator-not-pneqnp]`) and GCT (VP≠VNP `[vnp-separation-not-pneqnp]`). The `cavalar-oliveira-2025` graph-cover route remains the page's P≠NP-adjacent candidate.

`[alman-liang-2025-rigidity-stuck-barrier]` — the linear-circuit-depth/rigidity sub-route has a precise "barrier just beyond reach": Alman-Liang (arXiv:2502.19580, Feb 2025) near-tight low-rank rigidity LB R_{H_n}(Θ(n)) ≥ 4^n·(1/2 − 2^{−O(n)}) (spectral over finite fields, optimal 1/2 constant) + hardness amplification (slight improvement ⟹ Razborov rigidity ⟹ PH^cc communication LB), explaining why rigidity is stuck at Ω(N²/r·log(N/r)) — the depth/linear-circuit analogue of the S1.a tight window `[s1a-antiprox-obstruction]`; the rigidity route is itself closed for the flagship (Hadamard not Valiant-rigid, Alman-Williams STOC 2017); Chukhin-Kulikov-Mihajlin-Smirnova (STACS 2026) give a conditional explicit-rigid-family under a uniform-nondeterministic-LB assumption.

`[direct-sum-is-depth-face-of-noncompositionality]` — THE LOAD-BEARING finding: the KRW direct-sum obstacle (`[meir-2023]`, Meir FOCS 2023) IS the depth-face of the non-compositionality construction lock `[balanced-point-non-compositional]` — the composed object does not decompose into its parts (SIZE axis: g must be simultaneously expensive ∧ small-gap non-compositionally via S1.a lifting; DEPTH axis: g∘f must behave as a direct sum but does not via KRW). **The genuinely new structural product: the non-compositionality lock is AXIS-INDEPENDENT — it blocks the size axis (S1.a lifting) and the depth axis (KRW direct-sum) alike**; the wall's lock is specifically a NON-COMPOSITIONAL construction, a sharpening of `[witness-needs-explicit-lb]` across both classical LB axes.

**Status after Cycle 28:** NO BREAKTHROUGH; `[honest-ceiling]` upheld. The product is the FIRST non-size axis (depth) barrier-profiled against the wall + the axis-independence of the non-compositionality lock (the direct-sum obstacle on the depth axis = the balanced-point non-compositionality on the size axis). Re-confirms `[no-missed-connection-moved-wall]` from the SEVENTH sub-surface (structural → measure → disjoint-pairs/proof-complexity → descriptive → GCT/algebraic → direct-attack-on-(A) → depth/KW-communication) — barrier-profiling the genuinely-side depth axis re-discovered the same non-compositionality lock on a new axis; the lock is a (non-compositional) CONSTRUCTION not a CONNECTION. (A) remains the single live thread (OPEN construction) `[a-remains-open-construction]` `[s1a-is-the-live-thread]`. All findings search/arXiv-summary-level, NOT PDF-line-verified. Wiki: 18 pages (no new page — `communication-kw` expanded in place), 44 sources. **SEVENTH LOOP, Cycle 3 (absolute Cycle 28) complete — 2 cycles remain (29-30) per the governing mandate unless the user redirects.**

## See also
All angle pages · [[mcsp-meta-complexity]] · [[novel-diagnoses]] · [[open-problems]]