# Notes — Birch and Swinnerton-Dyer Conjecture

Scratch / sparks / dead ends. Promote good ones into attempts.

- **Cross-problem methodology**: the "obstruction at the control/reduction
  step, not the resolution step" lens worked for Beal [[beals_conjecture]] and
  applies cleanly to BSD. Worth recording as a reusable *methodology* page?
  (Not a math theorem, but a research strategy that compounds across problems.)

- **Analogy table (to develop)**: Beal's "reduction to finitely many curves" ↔
  BSD's "Selmer-group bound"; Beal's "every effective method needs an even
  exponent or near-spherical structure" ↔ BSD's "every rank-≥2 method needs a
  higher-rank Euler system".

- **Spark**: does the parity theorem + a *conditional* Sha-finiteness give
  anything unconditional at rank $\ge2$ via $p$-parity? Probably not (parity is
  already the ceiling), but worth a focused check.

- **Spark (refined front)**: the refined-BSD / Mazur-Tate direction (C) is
  *independent* of the rank direction (A/B) — it concerns the leading
  coefficient. A future cycle could split the attack into the "rank" sub-problem
  and the "leading coefficient" sub-problem, since they have different
  obstructions.